// ABOUTME: Next.js configuration for blakepetersen.io.
// ABOUTME: Minimal config -- extended in later phases.

import type { NextConfig } from 'next'

const config: NextConfig = {
  reactStrictMode: true,
}

export default config
