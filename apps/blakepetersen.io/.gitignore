/.idea
/.next
/node_modules
/out

.DS_Store
.env.local
*.log

/config/*.json
!/config/sample.json

# Pagefind search index (generated at build time)
/public/pagefind/

# Registry JSON endpoints (generated at build time)
/public/r/

.vercel

# Playwright runtime output (committed baselines live in tests/visual/*.spec.ts-snapshots/)
/playwright-report/
/test-results/

# Yarn 2 w/ Zero-Installs
.yarn/*
!.yarn/cache
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/sdks
!.yarn/versions