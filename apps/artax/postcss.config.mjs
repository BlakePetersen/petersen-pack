// ABOUTME: PostCSS configuration for the Artax UI reference site.
// ABOUTME: Uses Tailwind CSS v4 PostCSS plugin for CSS processing.
export default {
  plugins: {
    '@tailwindcss/postcss': {}
  }
}
