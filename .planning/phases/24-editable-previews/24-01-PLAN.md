---
phase: 24-editable-previews
plan: 1
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/artax/package.json
  - apps/artax/src/app/_spike/react-live-compat/page.tsx
  - .planning/phases/24-editable-previews/24-01-SPIKE-RESULT.md
autonomous: false
requirements: [ARTAX-08]
tags: [spike, react-live, react-19, compat-check]
must_haves:
  truths:
    - "react-live 4.1.8 renders a Button with variants under React 19 without runtime failure"
    - "RESEARCH.md Open Question 1 (React 19 JSX warning in dev + next build output) is answered with evidence"
    - "RESEARCH.md Open Question 2 (artax-ui wildcard scope tree-shaking) is answered with evidence"
    - "Spike result (PASS/FAIL) is documented in 24-01-SPIKE-RESULT.md and propagated to STATE.md blockers"
  artifacts:
    - path: "apps/artax/src/app/_spike/react-live-compat/page.tsx"
      provides: "Throwaway spike route rendering Button via LiveProvider/LivePreview"
      min_lines: 30
    - path: ".planning/phases/24-editable-previews/24-01-SPIKE-RESULT.md"
      provides: "Spike verdict: PASS or FAIL with evidence (console logs, build output, bundle size delta)"
      contains: "VERDICT:"
  key_links:
    - from: "apps/artax/src/app/_spike/react-live-compat/page.tsx"
      to: "react-live"
      via: "dynamic import + LiveProvider scope"
      pattern: "LiveProvider.*scope"
    - from: "24-01-SPIKE-RESULT.md"
      to: "downstream plans 24-06"
      via: "VERDICT field drives whether plan 24-06 executes"
      pattern: "VERDICT: (PASS|FAIL)"
---

<objective>
Run the react-live React 19 compatibility spike. Install react-live in apps/artax, render one registry component (Button) through LiveProvider/LivePreview on a throwaway route, and produce a PASS/FAIL verdict documenting evidence for both RESEARCH.md open questions.

Purpose: Plan 24-06 (JSX editor integration) is gated on this outcome. A PASS unblocks JSX editing; a FAIL triggers the spike-fail branch where the phase ships props-form only and defers JSX editing to ARTAX-F01.

Output: A throwaway spike route, a documented verdict file, and a go/no-go decision on react-live for this phase.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/24-editable-previews/24-CONTEXT.md
@.planning/phases/24-editable-previews/24-RESEARCH.md

@apps/artax/package.json
@apps/artax/src/lib/component-registry.ts
</context>

<interfaces>
<!-- react-live exports (from RESEARCH.md Pattern 1) — executor does not need to explore -->

From 'react-live':
```typescript
export function LiveProvider(props: {
  code: string
  scope?: Record<string, unknown>
  theme?: PrismTheme
  language?: string
  noInline?: boolean
  transformCode?: (code: string) => string
  children: React.ReactNode
}): JSX.Element

export function LiveEditor(props?: { className?: string }): JSX.Element
export function LivePreview(props?: { Component?: React.ComponentType }): JSX.Element
export function LiveError(props?: { className?: string }): JSX.Element
```

From 'artax-ui' (to be used in scope):
```typescript
export { Button } // plus 40+ other named exports
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Install react-live and create spike route</name>
  <files>apps/artax/package.json, apps/artax/src/app/_spike/react-live-compat/page.tsx</files>
  <read_first>
    - apps/artax/package.json (current deps)
    - apps/artax/src/lib/component-registry.ts (for Button scope reference)
    - .planning/phases/24-editable-previews/24-RESEARCH.md Pattern 1 (LiveProvider usage)
  </read_first>
  <action>
    1. Run `pnpm --filter artax add react-live@^4.1` from repo root. Verify apps/artax/package.json gains `"react-live": "^4.1.x"` in dependencies (exact minor resolved by pnpm; accept 4.1.8).
    2. Create apps/artax/src/app/_spike/react-live-compat/page.tsx with these exact requirements:
       - First two lines: `// ABOUTME: Throwaway spike route for react-live React 19 compatibility check.` and `// ABOUTME: Delete after plan 24-01 verdict is recorded.`
       - Third line: `'use client'`
       - Import LiveProvider, LivePreview, LiveError from 'react-live'
       - Import Button from 'artax-ui' directly (explicit import, NOT wildcard — that is a separate probe below)
       - Also perform `import * as artaxUI from 'artax-ui'` — log `Object.keys(artaxUI).length` to the console on mount via a `useEffect`
       - Default-export a React component named `ReactLiveSpikePage` that renders:
         - `<h1>react-live spike</h1>`
         - A LiveProvider with `code="<Button>spike</Button>"`, `scope={{ Button }}`, and a child `<LivePreview />` followed by `<LiveError />`
         - A second LiveProvider below the first with `code="<Button variant=\"outline\">wildcard-scope</Button>"`, `scope={{ ...artaxUI }}`, `<LivePreview />`, `<LiveError />`
    3. Do not add the spike route to any nav or sitemap. It is reachable only via direct URL `/_spike/react-live-compat` in dev.
  </action>
  <verify>
    <automated>pnpm --filter artax typecheck &amp;&amp; test -f apps/artax/src/app/_spike/react-live-compat/page.tsx</automated>
  </verify>
  <done>apps/artax/package.json lists react-live under dependencies; spike route file exists; `pnpm --filter artax typecheck` passes.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Blake runs dev + prod build and records verdict</name>
  <what-built>
    A throwaway spike route at /_spike/react-live-compat that renders Button twice through react-live — once with a narrow `{ Button }` scope, once with a wildcard `{ ...artaxUI }` scope.
  </what-built>
  <how-to-verify>
    1. Start dev: `pnpm --filter artax dev` (webpack flag is already in the dev script per PROJECT.md).
    2. Open http://localhost:3000/_spike/react-live-compat in Chrome with DevTools console open.
    3. Confirm both Buttons render (no blank spaces, no LiveError surface).
    4. Capture the full console output. Note specifically:
       - Any "Your app (or one of its dependencies) is using an outdated JSX transform" warning (RESEARCH.md Pitfall 1)
       - Any React 19 hydration warnings
       - Any Sucrase parse errors
       - The `Object.keys(artaxUI).length` log value
    5. Stop dev, run `pnpm --filter artax build`. Capture full stdout+stderr. Note whether the outdated-JSX-transform warning appears in production build output.
    6. Bundle-size probe for Open Question 2: read the build output's First Load JS size for the `/_spike/react-live-compat` route. Compare to any other route's First Load JS (e.g. `/components/atoms/button`). A wildcard scope that defeats tree-shaking would pull in a larger delta than a narrow scope.
    7. Write .planning/phases/24-editable-previews/24-01-SPIKE-RESULT.md with this exact structure:
       ```markdown
       # 24-01 Spike Result

       **Date:** YYYY-MM-DD
       **react-live version:** {from pnpm list}
       **React version:** 19.2.4

       ## VERDICT: PASS | FAIL

       ## Open Question 1 — React 19 JSX transform warning
       - **Dev console:** {exact message or "none observed"}
       - **Next build output:** {exact message or "none observed"}
       - **Classification:** {dev-only warning / prod warning / thrown error / not reproduced}

       ## Open Question 2 — artax-ui wildcard scope tree-shaking
       - **Object.keys(artaxUI).length:** {number}
       - **/_spike/react-live-compat First Load JS:** {size from build}
       - **/components/atoms/button First Load JS:** {size from build}
       - **Assessment:** {safe to spread / switch to named scope / inconclusive}

       ## Render behavior
       - Narrow-scope Button rendered: yes/no
       - Wildcard-scope Button rendered: yes/no
       - Keystroke re-render tested (if time): yes/no

       ## Recommendation
       - 24-06 JSX editor integration: PROCEED | DEFER TO ARTAX-F01
       - Scope strategy: spread artax-ui | enumerate named exports
       - Warning handling: tolerate | suppress via transformCode | investigate PR #406
       ```
    8. If VERDICT is PASS: delete the spike route file and run `pnpm --filter artax typecheck` to confirm clean.
    9. If VERDICT is FAIL: keep the spike route file present in a commit on a branch named `spike/24-01-fail` for post-mortem reference, then delete from main before the next plan starts.
    10. Update .planning/STATE.md Blockers/Concerns: either remove the "react-live React 19 compat" line (PASS) or update it with the FAIL summary and ARTAX-F01 deferral note.
  </how-to-verify>
  <resume-signal>Type "pass" (if VERDICT=PASS, all downstream plans including 24-06 proceed) or "fail" (if VERDICT=FAIL, plan 24-06 is skipped; execute-plan marks it `gap_closure_mode: spike-fail` and the phase closes at plan 24-07 with VERIFICATION.md recording partial ARTAX-08 satisfaction).</resume-signal>
</task>

</tasks>

<verification>
- `pnpm --filter artax typecheck` passes after spike route is deleted (PASS) or quarantined (FAIL).
- 24-01-SPIKE-RESULT.md exists and contains `VERDICT: PASS` or `VERDICT: FAIL` as the first heading after the metadata block.
- STATE.md Blockers/Concerns updated to reflect the outcome.
- If FAIL: a commit exists on branch `spike/24-01-fail` preserving the spike route; main has the spike route removed.
</verification>

<success_criteria>
- RESEARCH.md Open Question 1 answered with captured console/build evidence.
- RESEARCH.md Open Question 2 answered with bundle-size evidence.
- A single, unambiguous VERDICT drives whether 24-06 runs.
- No spike code remains on main after the verdict is recorded.
</success_criteria>

<output>
After completion, create `.planning/phases/24-editable-previews/24-01-SUMMARY.md` summarizing: verdict, evidence snippets (5-10 lines each), the scope strategy recommendation that 24-06 should adopt, and whether STATE.md was updated.
</output>
