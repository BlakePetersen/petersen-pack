---
phase: 24-editable-previews
plan: 7
type: execute
wave: 5
depends_on: [5]
files_modified:
  - apps/artax/tests/component-routes.test.ts
  - apps/artax/tests/playground-routes.test.ts
autonomous: true
requirements: [ARTAX-08]
tags: [route-tests, integration, url-round-trip, regression]
must_haves:
  truths:
    - "All 15 component routes still build and pass existing regression tests"
    - "Navigating to `/components/atoms/button?p[variant]=outline&p[size]=sm` causes the Playground tab's form to initialize with those values (URL → state round-trip)"
    - "The 4 excluded components (Dialog, Dropdown, Tooltip, Accordion) do NOT render a Playground tab trigger in their pages"
    - "The 11 enabled components DO render a Playground tab trigger"
    - "generateStaticParams in apps/artax/src/app/components/[tier]/[slug]/page.tsx still returns 15 entries after registry changes (regression)"
  artifacts:
    - path: "apps/artax/tests/playground-routes.test.ts"
      provides: "Route-level integration tests for Playground tab visibility and URL hydration"
      contains: "describe('playground routes'"
    - path: "apps/artax/tests/component-routes.test.ts"
      provides: "Extended regression — 15 static params, no drift"
      contains: "generateStaticParams"
  key_links:
    - from: "tests/playground-routes.test.ts"
      to: "component-registry.ts exclusion list + component-page-client.tsx tab conditional"
      via: "render ComponentPageClient with slug, assert tab presence/absence"
      pattern: "Playground"
---

<objective>
Lock in phase behavior with route-level integration tests: URL round-trip hydration, tab visibility per component, and regression coverage for the 15-entry static params output. Works whether or not plan 24-06 ran.

Purpose: Give the phase gate a pure `pnpm --filter artax test` check that catches future drift (e.g., someone accidentally adds/removes playground enablement, breaks URL decoding, or modifies generateStaticParams).

Output: One new route test suite and a small extension to the existing component-routes regression suite.

Note on task classification: these tasks are integration/regression tests written AGAINST already-shipped code from 24-05 (and optionally 24-06). They are NOT TDD — there is no RED→GREEN→REFACTOR cycle because the implementation is already locked. Tests should pass on first run; a failure is signal to investigate, not an invitation to loosen the test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/24-editable-previews/24-CONTEXT.md
@.planning/phases/24-editable-previews/24-RESEARCH.md

@apps/artax/src/components/component-page-client.tsx
@apps/artax/src/components/component-playground.tsx
@apps/artax/src/lib/component-registry.ts
@apps/artax/src/lib/playground-url-state.ts
@apps/artax/tests/component-routes.test.ts
@apps/artax/jest.setup.ts
</context>

<interfaces>
<!-- No new module interfaces — this plan is tests only -->
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Add playground-routes integration test suite</name>
  <files>apps/artax/tests/playground-routes.test.ts</files>
  <read_first>
    - apps/artax/tests/component-routes.test.ts (existing route-test style)
    - apps/artax/src/components/component-page-client.tsx (final integrated version post-24-05)
    - apps/artax/src/lib/component-registry.ts (to iterate enabled vs excluded slugs)
    - .planning/phases/24-editable-previews/24-CONTEXT.md D-05 (exclusion list)
    - apps/artax/jest.setup.ts (jest-dom matchers already wired by plan 24-04)
  </read_first>
  <behavior>
    - These are integration/regression tests written AGAINST already-shipped code from plans 24-05 and 24-06. This is not TDD — the implementation is locked; the tests lock in observable behavior.
    - File starts with `/** @jest-environment jsdom */` + two `// ABOUTME:` lines
    - Mock next/navigation's useSearchParams to return controlled URLSearchParams per test
    - Test: for each slug in the 11-enabled list (enumerated in a local array matching plan 24-03's ENABLED_PLAYGROUND_SLUGS), rendering `&lt;ComponentPageClient tier={...} slug={...} /&gt;` produces a TabsTrigger with text "Playground" (query by role or text)
    - Test: for each slug in the 4-excluded list (`tooltip`, `accordion`, `dialog`, `dropdown`), rendering produces NO TabsTrigger with text "Playground" (assert absence)
    - Test: Round-trip hydration — useSearchParams returns `new URLSearchParams('p[variant]=outline')`, render ComponentPageClient for slug 'button', programmatically click the Playground tab, assert the native &lt;select name="variant"&gt; in the form has value 'outline'. (If switching the Tabs value requires interaction support — use `user-event` or set `defaultValue="playground"` via a test wrapper if `Tabs` state is client-only. Fall back to rendering `&lt;ComponentPlayground comp={...} /&gt;` directly if the Tabs wrapping causes jsdom flakiness.)
    - Test: Round-trip hydration for the Size prop — `p[size]=sm` → size select value 'sm' after mount.
    - Test: URL with a key that does not match any registered prop (e.g., `p[bogus]=x`) does not crash and does not render an input for `bogus`.
  </behavior>
  <action>
    1. Create apps/artax/tests/playground-routes.test.ts. These are integration/regression tests — not RED→GREEN→REFACTOR TDD. The implementation from plans 24-05 (ComponentPlayground + tab wiring) and 24-06 (JSX editor, if spike passed) is already shipped. This plan's job is to pin observable behavior so future drift is caught.
    2. Start with the docblock + ABOUTME header. Mirror the mock setup from apps/artax/tests/component-playground.test.tsx (plan 24-05). @testing-library/jest-dom matchers are available via apps/artax/jest.setup.ts (wired in plan 24-04) — no per-file import needed.
    3. Define local constants (duplicate of plan 24-03 test arrays is acceptable — locality of test intent matters more than DRY for exclusion-list pinning):
       ```ts
       const ENABLED = ['button', 'input', 'badge', 'separator', 'copy-button', 'toggle', 'card', 'table', 'callout', 'code-block', 'tabs']
       const EXCLUDED = ['tooltip', 'accordion', 'dialog', 'dropdown']
       ```
    4. Use `describe.each(ENABLED)` and `describe.each(EXCLUDED)` blocks for the per-slug tab-presence tests. Resolve tier for each slug via `getComponent` from the registry — keeps tests aligned with the real mapping.
    5. For the URL round-trip test, import ComponentPlayground directly (avoids Tabs-state interaction complexity):
       ```ts
       mockedUseSearchParams.mockReturnValue(new URLSearchParams('p[variant]=outline'))
       render(&lt;ComponentPlayground comp={getComponent('atoms', 'button')!} /&gt;)
       const select = screen.getByRole('combobox', { name: /variant/i })
       expect(select).toHaveValue('outline')
       ```
    6. Run `pnpm --filter artax test -- playground-routes` — since the underlying implementation from 24-05 already exists, this should be GREEN on first run. If a test fails, determine whether the expectation is wrong (fix test) or the implementation has a defect (fix implementation). Do not blindly modify tests to pass — a failing regression test is signal, not noise.
    7. Commit `test(24-07): add playground-routes regression suite`.
  </action>
  <verify>
    <automated>pnpm --filter artax test -- playground-routes</automated>
  </verify>
  <done>Test file exists with ABOUTME + docblock; every enabled slug has a passing tab-presence test; every excluded slug has a passing tab-absence test; URL round-trip test passes; no fake-RED scaffolding present (this is a regression suite, not a TDD cycle).</done>
</task>

<task type="auto">
  <name>Task 2: Extend component-routes regression coverage</name>
  <files>apps/artax/tests/component-routes.test.ts</files>
  <read_first>
    - apps/artax/tests/component-routes.test.ts (current regression coverage)
    - apps/artax/src/app/components/[tier]/[slug]/page.tsx (generateStaticParams)
  </read_first>
  <behavior>
    - Existing assertion that generateStaticParams returns 15 params remains intact and passing
    - NEW assertion: `getAllComponents().length === 15` (double-lock on registry size so future work cannot silently trim it)
    - NEW assertion: The set of slugs returned by generateStaticParams matches the set of slugs in getAllComponents (no drift)
  </behavior>
  <action>
    1. Open apps/artax/tests/component-routes.test.ts. Locate the existing generateStaticParams test.
    2. Add a new `describe('registry size guard', () =&gt; { ... })` block:
       - `it('registry contains exactly 15 components', ...)` — reads getAllComponents, asserts length 15.
       - `it('static params match registry slugs', ...)` — awaits `generateStaticParams()`, maps to `${tier}/${slug}`, compares to getAllComponents().map(c =&gt; `${c.tier}/${c.slug}`) as sets.
    3. Do NOT remove any existing assertions.
    4. Run `pnpm --filter artax test -- component-routes` — confirm all tests (existing + new) pass. If a test fails, determine whether the expectation is wrong (fix test) or the implementation has a defect (fix implementation). Do not blindly modify tests to pass.
  </action>
  <verify>
    <automated>pnpm --filter artax test -- component-routes</automated>
  </verify>
  <done>New assertions pass; old assertions pass; `grep -c "toHaveLength(15)" apps/artax/tests/component-routes.test.ts` returns at least 1 match.</done>
</task>

<task type="auto">
  <name>Task 3: Full suite + coverage gate</name>
  <files>(no source edits — verification only)</files>
  <read_first>
    - apps/artax/jest.config.ts
  </read_first>
  <action>
    1. Run `pnpm --filter artax test --coverage` from the repo root.
    2. Capture the coverage summary. Record in the plan SUMMARY the percentages for `src/lib/playground-url-state.ts`, `src/lib/playground-prop-coercion.ts`, `src/components/playground-props-form.tsx`, and `src/components/component-playground.tsx`.
    3. If any of the four has less than 80% line coverage, add one more targeted test in the existing suite to close the gap. Re-run coverage to confirm.
    4. Run `pnpm --filter artax build` to confirm production build still succeeds with all new code.
    5. Run `pnpm --filter artax lint` (if a lint script exists — check apps/artax/package.json; if present, run it).
  </action>
  <verify>
    <automated>pnpm --filter artax test --coverage &amp;&amp; pnpm --filter artax build</automated>
  </verify>
  <done>Full Jest suite green with coverage report captured; all four new modules at &gt;=80% line coverage; production build succeeds.</done>
</task>

</tasks>

<verification>
- `pnpm --filter artax test` passes end-to-end (playground + all prior suites).
- `pnpm --filter artax typecheck` passes.
- `pnpm --filter artax build` passes.
- Coverage numbers for the four new modules recorded in the plan SUMMARY.
- Exclusion list is now pinned in THREE places: source (by omission in component-registry.ts), plan 24-03 tests, plan 24-07 tests — drift in any one will be caught.
</verification>

<success_criteria>
- URL round-trip verified via integration test.
- Tab-presence matrix (11 enabled / 4 excluded) verified via integration test.
- Registry size regression guard in place.
- Coverage gate satisfied for new modules.
- Phase is ready for /gsd-verify-work.
</success_criteria>

<output>
After completion, create `.planning/phases/24-editable-previews/24-07-SUMMARY.md` with coverage numbers for the four new modules, a summary of the 15-slug matrix asserted, the outcome of `pnpm --filter artax build`, and a note on whether plan 24-06 executed (PASS branch) or was skipped (FAIL branch).
</output>
