---
phase: 26-blakepetersen-io-page-updates
plan: 04
type: execute
wave: 2
depends_on: [01]
autonomous: false
requirements: [SITE-05]
requirements_addressed: [SITE-05]
tags: [bp.io, about, site-05, badge]
files_modified:
  - apps/blakepetersen.io/src/app/about/page.tsx
must_haves:
  truths:
    - "About page composition matches the Pencil About frame in both light and dark modes"
    - "Badge chips render the name/role/location row and skills/interests summary using the artax-ui Badge primitive (not inline spans)"
    - "Contact CTAs use the shell-command convention ($ email-blake, $ find-me-on-github) per UI-SPEC"
    - "Optional AuthorNote wraps the personal-voice section IF the Pencil frame shows it (per D-05 and Plan 01 outcome)"
    - "No hardcoded color literals introduced; Phase 25 audit remains canonical"
  artifacts:
    - path: "apps/blakepetersen.io/src/app/about/page.tsx"
      provides: "Static About page with Pencil-matched composition, Badge chips, terminal CTAs"
      contains: "$ email-blake"
  key_links:
    - from: "apps/blakepetersen.io/src/app/about/page.tsx"
      to: "artax-ui Badge"
      via: "import { Badge } from 'artax-ui'"
      pattern: "import \\{ Badge"
---

<objective>
Surgically rewrite `apps/blakepetersen.io/src/app/about/page.tsx` to match the Pencil About frame. Introduce artax-ui `Badge` chips for the name/role/location row and skills/interests summary, apply UI-SPEC reading column width (`max-w-prose` centered), and add terminal-convention contact CTAs. Optionally wrap a personal-voice section in `AuthorNote` if the Pencil frame shows it (subject to Plan 01 Task 4 outcome).

Purpose: Delivers SITE-05.
Pre-condition: Plan 01 shipped (Badge extended; AuthorNote present per Task 4 outcome). Pencil desktop app running.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/26-blakepetersen-io-page-updates/26-CONTEXT.md
@.planning/phases/26-blakepetersen-io-page-updates/26-UI-SPEC.md
@.planning/phases/26-blakepetersen-io-page-updates/26-RESEARCH.md
@.planning/phases/26-blakepetersen-io-page-updates/26-PATTERNS.md
@.planning/phases/26-blakepetersen-io-page-updates/26-01-SUMMARY.md

<interfaces>
From artax-ui:
```ts
import { Badge } from 'artax-ui'        // variants: default | outline | secondary | info | success | warning | destructive
import { AuthorNote } from 'artax-ui'   // available IF Plan 01 Task 4 selected option-a or option-b
```

Shell-command CTA motif (from apps/blakepetersen.io/src/app/page.tsx lines 125-129):
```tsx
<a href="..." className="text-primary hover:underline">$ verb-noun</a>
```

Bracket CTA motif (from Homepage, for reference):
```tsx
<Link href="/..." className="text-primary hover:underline">[label]</Link>
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: About page Pencil-matched recompose</name>
  <files>apps/blakepetersen.io/src/app/about/page.tsx</files>
  <read_first>
    - apps/blakepetersen.io/src/app/about/page.tsx (full) — current static layout
    - .planning/phases/26-blakepetersen-io-page-updates/26-UI-SPEC.md §"About" — layout, chip row, contact CTAs, optional AuthorNote
    - .planning/phases/26-blakepetersen-io-page-updates/26-PATTERNS.md §"apps/blakepetersen.io/src/app/about/page.tsx" — preserve list + additions
    - .planning/phases/26-blakepetersen-io-page-updates/26-01-SUMMARY.md — confirm AuthorNote placement (option-a/b/c/d affects availability)
    - apps/blakepetersen.io/src/app/page.tsx lines 125-129 — shell-command CTA pattern to copy verbatim
  </read_first>
  <action>
    Use `mcp__pencil__get_screenshot "About Page"` to drive composition judgment. For ambiguity on chip labels, `mcp__pencil__batch_get` patterns=`['About Page']`.

    **Preserve verbatim (D-01):**
    - `// ABOUTME:` 2-line header at top
    - Any existing static content structure that Pencil does not override (the 3-section split around `// about` / philosophy / this_project captions if present in current impl)
    - All `// section_name` captions that appear in current code

    **Recompose per UI-SPEC line 232-240:**

    1. Layout shell: single centered reading column. Use `max-w-prose` (~65ch) with `mx-auto px-4 py-12`. If current impl uses `max-w-[80ch]`, switch to `max-w-prose` unless Pencil frame explicitly shows wider. Root wrapper stays the layout-level Header/Footer (from `apps/blakepetersen.io/src/app/layout.tsx`).

    2. Header section (UI-SPEC §About lines 238):
       ```tsx
       <p className="mb-4 font-mono text-xs text-muted-foreground">{'// about'}</p>
       <h1 className="mb-6 font-mono-alt text-3xl leading-tight">{/* name or tagline */}</h1>

       <div className="mb-8 flex flex-wrap gap-2">
         <Badge variant="outline">{/* name */}</Badge>
         <Badge variant="outline">{/* role */}</Badge>
         <Badge variant="outline">{/* location */}</Badge>
       </div>
       ```
       Pull chip label text from the Pencil frame exactly. Do NOT fabricate labels.

    3. Biographical prose: `font-mono text-lg leading-relaxed` paragraphs. Pull body text from the current page.tsx verbatim — this plan is a visual recompose; copy edits are out of scope unless the Pencil frame shows explicit prose replacement.

    4. Skills / interests summary (UI-SPEC line 239): a Badge grid below the bio, same `flex flex-wrap gap-2` pattern, `variant="secondary"` or `variant="outline"` per Pencil.

    5. Contact CTAs (UI-SPEC line 240 + Copywriting Contract):
       ```tsx
       <div className="mt-8 space-y-2 font-mono text-base">
         <div><a href="mailto:blake@blakepetersen.io" className="text-primary hover:underline">$ email-blake</a></div>
         <div><a href="https://github.com/blakepetersen" className="text-primary hover:underline">$ find-me-on-github</a></div>
       </div>
       ```
       Use the email/GitHub URLs from the current impl; the `$ verb-noun` label is prescribed by UI-SPEC.

    6. **Optional AuthorNote** (UI-SPEC line 240 conditional): If the Pencil About frame wraps a personal-voice section in the note motif, use `<AuthorNote>` from artax-ui to wrap that paragraph. Pull the byline (if Pencil shows one) into the `author` prop. Skip if Pencil does not show a note.

       If Plan 01 Task 4 selected option-c (AuthorNote stays bp.io-local), import from `../../components/author-note` instead.

    **Tokens only** — no hardcoded color literals. Regression guard:
    ```bash
    grep -E 'bg-(amber|cyan|emerald|red|zinc)-[0-9]' apps/blakepetersen.io/src/app/about/page.tsx
    ```
    Must return empty.

    **Server component check:** About is static. Confirm `head -3 apps/blakepetersen.io/src/app/about/page.tsx` does NOT start with `'use client'`.
  </action>
  <verify>
    <automated>pnpm --filter blakepetersen.io typecheck && pnpm --filter blakepetersen.io build</automated>
  </verify>
  <acceptance_criteria>
    - `apps/blakepetersen.io/src/app/about/page.tsx` does NOT start with `'use client'`
    - `apps/blakepetersen.io/src/app/about/page.tsx` contains `import { Badge } from 'artax-ui'`
    - `apps/blakepetersen.io/src/app/about/page.tsx` contains `<Badge` used at least 3 times (name/role/location chip row at minimum)
    - `apps/blakepetersen.io/src/app/about/page.tsx` contains `$ email-blake` AND `$ find-me-on-github`
    - `apps/blakepetersen.io/src/app/about/page.tsx` contains `// about` section caption
    - `apps/blakepetersen.io/src/app/about/page.tsx` contains `max-w-prose` OR `max-w-[80ch]` (reading column width present)
    - `apps/blakepetersen.io/src/app/about/page.tsx` does NOT contain a hardcoded `mailto:` or GitHub URL that's different from the current impl's (URLs are preserved, only chrome changes)
    - `grep -E 'bg-(amber|cyan|emerald|red|zinc)-[0-9]' apps/blakepetersen.io/src/app/about/page.tsx` returns empty
    - `pnpm --filter blakepetersen.io typecheck` exits 0
    - `pnpm --filter blakepetersen.io build` succeeds
  </acceptance_criteria>
  <done>About page matches Pencil frame; Badge chips for meta row; terminal CTAs; optional AuthorNote per Pencil; server-component status preserved; build clean.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: D-07 light/dark smoke check (About)</name>
  <what-built>About page recompose: Badge chip rows, shell-command contact CTAs, reading column width, optional AuthorNote wrap per Pencil.</what-built>
  <how-to-verify>
    1. `pnpm --filter blakepetersen.io dev`.
    2. Open `http://localhost:3000/about`.
    3. DARK MODE (default):
       - `// about` section caption in `text-muted-foreground`.
       - H1 editorial (`font-mono-alt`), `text-3xl`.
       - Name/role/location Badge row — outlined chips, mono, `text-xs`.
       - Biographical prose in mono, `text-lg leading-relaxed`.
       - Skills/interests Badge grid below prose.
       - Contact CTAs: `$ email-blake` and `$ find-me-on-github`, primary-colored, underline on hover.
       - mailto + GitHub links resolve to current targets (cmd-click to verify).
       - If AuthorNote present: left border, `// author_note` caption, inset content.
    4. LIGHT MODE: same, contrast adequate, accent still reserved to CTAs.
    5. Toggle dark ↔ light multiple times — no flicker, no hydration warnings.
    6. 375px / 768px / 1280px — column stays centered, chips wrap cleanly.
    7. Keyboard: Tab through CTAs, focus-visible ring visible.
    8. Compare against Pencil About frame via `mcp__pencil__get_screenshot "About Page"` — section order + chip labels + CTA copy match.

    Type "approved" if all pass.
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| server (SSG)→HTML | About is static; no data fetching, no user input. |
| external link→user | `mailto:` and GitHub URL are hardcoded, known-safe destinations. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-26-04-01 | Information Disclosure | Open-redirect via contact href | accept | hrefs are hardcoded constants (`mailto:blake@…`, `https://github.com/blakepetersen`). No user input crosses into any link target. |
| T-26-04-02 | Tampering | Color-literal regression | mitigate | Grep guard in acceptance criteria ensures no `bg-(amber\|cyan\|emerald\|red\|zinc)-N` added. |
</threat_model>

<verification>
1. typecheck + build clean.
2. Task 2 D-07 smoke check approved.
3. No color-literal regressions.
4. Page remains a server component (no `'use client'`).
</verification>

<success_criteria>
- About page visually matches Pencil in both themes.
- Badge chips + shell CTAs composed per UI-SPEC.
- Build clean.
- D-07 approved.
</success_criteria>

<output>
`.planning/phases/26-blakepetersen-io-page-updates/26-04-SUMMARY.md` — Pencil-driven composition choices, whether AuthorNote was used, smoke-check notes.
</output>
