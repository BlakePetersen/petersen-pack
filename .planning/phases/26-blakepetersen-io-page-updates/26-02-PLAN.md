---
phase: 26-blakepetersen-io-page-updates
plan: 02
type: execute
wave: 2
depends_on: [01]
autonomous: false
requirements: [SITE-03]
requirements_addressed: [SITE-03]
tags: [bp.io, homepage, site-03, badge, modal]
files_modified:
  - apps/blakepetersen.io/src/app/page.tsx
  - apps/blakepetersen.io/src/components/home-subscribe-modal.tsx
must_haves:
  truths:
    - "Homepage preserves getCollection('posts') and getAllCollections() data contract (no client-ification of page.tsx)"
    - "Homepage uses Badge (variant='outline') for stack chips instead of inline span with border-border px-2 py-1"
    - "Homepage renders cleanly in both light and dark modes per D-07"
    - "Pencil Homepage frame composition matches (section order, caption motifs, bracket/shell CTA conventions)"
  artifacts:
    - path: "apps/blakepetersen.io/src/app/page.tsx"
      provides: "Homepage server component with updated stack chips and Pencil-matched composition"
      contains: "getAllCollections"
    - path: "apps/blakepetersen.io/src/components/home-subscribe-modal.tsx"
      provides: "Optional client island wrapping Modal if Pencil Homepage frame shows a contribute/subscribe modal"
      contains: "'use client'"
  key_links:
    - from: "apps/blakepetersen.io/src/app/page.tsx"
      to: "artax-ui Badge"
      via: "import { Badge } from 'artax-ui'"
      pattern: "import \\{ Badge"
    - from: "apps/blakepetersen.io/src/app/page.tsx"
      to: "apps/blakepetersen.io/src/lib/collection-registry.ts"
      via: "getAllCollections + getCollection('posts')"
      pattern: "getAllCollections|getCollection\\('posts'\\)"
---

<objective>
Surgically rewrite `apps/blakepetersen.io/src/app/page.tsx` to match the Pencil Homepage frame while preserving all data fetching (`getCollection('posts').getter()`, `getAllCollections()`, `stackTools`), the `max-w-[1600px]` container, and the 5-section structure (`// dx_workbench`, `// stack`, `// collections`, `// recent_posts`, `// contribute`). Replace inline stack-chip spans with the new `Badge variant="outline"`. If (and only if) the Pencil frame shows a subscribe/contact modal off `// contribute`, introduce a client-island wrapper to keep `page.tsx` as a server component.

Purpose: Delivers SITE-03.
Pre-condition: Pencil desktop app running (executor uses `mcp__pencil__get_screenshot Homepage` to resolve per-section composition details).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/26-blakepetersen-io-page-updates/26-CONTEXT.md
@.planning/phases/26-blakepetersen-io-page-updates/26-UI-SPEC.md
@.planning/phases/26-blakepetersen-io-page-updates/26-RESEARCH.md
@.planning/phases/26-blakepetersen-io-page-updates/26-PATTERNS.md
@.planning/phases/26-blakepetersen-io-page-updates/26-01-SUMMARY.md

<interfaces>
From artax-ui barrel (available after Plan 01 ships):
```ts
import { Badge } from 'artax-ui'         // variants: default | outline | secondary | info | success | warning | destructive
import { Modal } from 'artax-ui'         // { open?, onOpenChange?, size?, trigger?, children, className? }; Modal.Title, Modal.Description, Modal.Close
```

From apps/blakepetersen.io/src/lib/collection-registry.ts (preserve):
```ts
getAllCollections(): CollectionMeta[]
getCollection(slug): { getter(): ContentItem[], ... }
```

Homepage current section anchors (preserve verbatim):
- Line 28: `// dx_workbench`
- Line 44: `// stack`
- Line 56: `// collections`
- Line 82: `// recent_posts`
- Line 119: `// contribute`
- Lines 34-37: `[skills] [hooks] [configs] [guides]` bracket CTAs
- Lines 125-129: `$ report-problem` / `$ suggest-improvement` shell CTAs
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pencil Homepage frame read + surgical recompose</name>
  <files>apps/blakepetersen.io/src/app/page.tsx, apps/blakepetersen.io/src/components/home-subscribe-modal.tsx</files>
  <read_first>
    - apps/blakepetersen.io/src/app/page.tsx (full — 137 lines) — current structure to surgically recompose
    - .planning/phases/26-blakepetersen-io-page-updates/26-UI-SPEC.md §"Homepage (`apps/blakepetersen.io/src/app/page.tsx`) — SITE-03" — per-section composition contract
    - .planning/phases/26-blakepetersen-io-page-updates/26-PATTERNS.md §"apps/blakepetersen.io/src/app/page.tsx" — preserve-verbatim list + Badge swap site
    - .planning/phases/26-blakepetersen-io-page-updates/26-RESEARCH.md Pitfall 3 "Client-Boundary Creep on Homepage"
    - .planning/phases/26-blakepetersen-io-page-updates/26-01-SUMMARY.md — Plan 01 outcome (primitive import paths, any variant deltas)
    - apps/blakepetersen.io/src/components/category-card.tsx — existing, reuse (do not recreate per UI-SPEC line 216)
    - apps/blakepetersen.io/src/components/sidebar-drawer.tsx — pattern reference for client-island import into server component
  </read_first>
  <action>
    Start by running `mcp__pencil__get_screenshot` for the Pencil `Homepage` frame to drive composition judgment. If the screenshot is ambiguous on any primitive's usage, use `mcp__pencil__batch_get` with patterns `['Homepage']` to extract structured content.

    **Preserve verbatim (D-01 surgical rewrite):**
    - `// ABOUTME:` 2-line header at the top
    - Imports of `getAllCollections`, `getCollection` from `../lib/collection-registry`
    - Module-scope `categories = getAllCollections()` and `stackTools = [...]` array
    - `recentPosts = getCollection('posts').getter().slice(0, N)` pattern
    - `mx-auto max-w-[1600px] px-4 py-8` container
    - All 5 section captions: `// dx_workbench`, `// stack`, `// collections`, `// recent_posts`, `// contribute`
    - `[skills]`, `[hooks]`, `[configs]`, `[guides]` bracket CTAs (lines 34-37)
    - `$ report-problem`, `$ suggest-improvement` shell CTAs (lines 125-129)
    - `if (recentPosts.length === 0) { /* omit */ }` empty-state behavior (line 80)
    - `CategoryCard` import + grid composition (do not recreate)
    - Date format `toLocaleDateString('en-US', { year: 'numeric', month: 'short', day: 'numeric' })` if present in recent posts render

    **Mandatory change (UI-SPEC line 214 + PATTERNS "Replace"):**
    Replace the inline stack-chip span at the `// stack` section (currently around lines 47-50):
    ```tsx
    <span className="border border-border px-2 py-1 font-mono text-xs text-secondary-foreground">{tool}</span>
    ```
    With:
    ```tsx
    <Badge variant="outline">{tool}</Badge>
    ```
    Add the import: `import { Badge } from 'artax-ui'` near the other imports.

    **Pencil-driven refinements (apply only what the frame shows):**
    - `// collections` grid: UI-SPEC specifies 2-column at `md+` with responsive wrap if `getAllCollections()` returns > 6. Verify against Pencil frame; adjust if Pencil shows 3-column.
    - `// recent_posts`: vertical list with H3 heading at `text-base font-medium`, muted meta in `text-muted-foreground text-xs`, `hover:border-primary` row hover. Adjust current markup to match.
    - Typography tokens: heading H1 `text-3xl font-mono-alt leading-tight`, section captions `text-xs text-muted-foreground`, body `text-base font-mono`. Do NOT introduce new size tokens outside the 4-size scale (`text-xs`, `text-base`, `text-lg`, `text-3xl`).
    - Colors: only semantic tokens — `bg-background`, `text-foreground`, `text-muted-foreground`, `bg-card`, `border-border`, `text-primary`, `hover:text-primary`, `hover:border-primary`. NO color literals.

    **Conditional Modal (ONLY if Pencil frame shows one):**
    If the Pencil Homepage frame wires a subscribe/contact Modal off `// contribute`:
    1. Create `apps/blakepetersen.io/src/components/home-subscribe-modal.tsx` as a `'use client'` island:
    ```tsx
    'use client'
    // ABOUTME: Client island wrapping Modal for the contribute/subscribe CTA.
    // ABOUTME: Keeps page.tsx as a server component so getCollection runs at build time.
    import { useState } from 'react'
    import { Modal } from 'artax-ui'

    export function HomeSubscribeModal() {
      const [open, setOpen] = useState(false)
      return (
        <Modal
          open={open}
          onOpenChange={setOpen}
          trigger={<button type="button" className="text-primary hover:underline">$ subscribe</button>}
        >
          <Modal.Title>subscribe</Modal.Title>
          <Modal.Description>{/* body from Pencil frame */}</Modal.Description>
          {/* form body or static content matching the Pencil frame */}
        </Modal>
      )
    }
    ```
    2. Import `HomeSubscribeModal` into `page.tsx` (NOT `Modal` directly — that would force `page.tsx` to `'use client'`). Render inside the `// contribute` section.
    3. `page.tsx` MUST NOT gain a `'use client'` directive. Confirm with `head -1 apps/blakepetersen.io/src/app/page.tsx` — should not equal `'use client'`.

    If Pencil does NOT show a Modal, skip the home-subscribe-modal.tsx file entirely (do not create empty file).

    **Regression guards the executor runs before declaring done:**
    - `grep -E 'bg-(amber|cyan|emerald|red|zinc)-[0-9]' apps/blakepetersen.io/src/app/page.tsx` returns empty.
    - `head -3 apps/blakepetersen.io/src/app/page.tsx` — first line is the `// ABOUTME:` comment, not `'use client'`.
    - `grep "border border-border px-2 py-1 font-mono text-xs" apps/blakepetersen.io/src/app/page.tsx` returns empty (old stack-chip span removed).
    - `grep "getAllCollections\|getCollection('posts')" apps/blakepetersen.io/src/app/page.tsx` still returns matches (data contract preserved).
  </action>
  <verify>
    <automated>pnpm --filter blakepetersen.io typecheck && pnpm --filter blakepetersen.io build 2>&1 | tee /tmp/bp-build.log && grep -q "Compiled successfully" /tmp/bp-build.log</automated>
  </verify>
  <acceptance_criteria>
    - `apps/blakepetersen.io/src/app/page.tsx` does NOT start with `'use client'` (server component preserved)
    - `apps/blakepetersen.io/src/app/page.tsx` contains `getAllCollections` AND `getCollection('posts')` (data contract preserved)
    - `apps/blakepetersen.io/src/app/page.tsx` contains `import { Badge } from 'artax-ui'`
    - `apps/blakepetersen.io/src/app/page.tsx` contains `<Badge variant="outline">` at the stack section
    - `apps/blakepetersen.io/src/app/page.tsx` does NOT contain the literal `border border-border px-2 py-1 font-mono text-xs` (old stack-chip span removed)
    - `apps/blakepetersen.io/src/app/page.tsx` contains all five section captions: `// dx_workbench`, `// stack`, `// collections`, `// recent_posts`, `// contribute`
    - `apps/blakepetersen.io/src/app/page.tsx` contains the bracket CTAs `[skills]`, `[hooks]`, `[configs]`, `[guides]`
    - `apps/blakepetersen.io/src/app/page.tsx` contains the shell CTAs `$ report-problem` AND `$ suggest-improvement`
    - `grep -E 'bg-(amber|cyan|emerald|red|zinc)-[0-9]' apps/blakepetersen.io/src/app/page.tsx` returns empty
    - `pnpm --filter blakepetersen.io typecheck` exits 0
    - `pnpm --filter blakepetersen.io build` succeeds with no hydration warnings in stderr
    - If `home-subscribe-modal.tsx` was created: first line is `'use client'` and it contains `import { Modal } from 'artax-ui'`
  </acceptance_criteria>
  <done>Homepage surgically matches Pencil frame; Badge swap applied; data contract preserved; page.tsx stays a server component; typecheck + build clean.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: D-07 light/dark smoke check (Homepage)</name>
  <what-built>Homepage rewrite: Pencil-matched section composition, Badge primitive for stack chips, all CTAs + captions preserved, data contract intact. Optional Modal subscribe island if Pencil showed one.</what-built>
  <how-to-verify>
    1. Start the dev server: `pnpm --filter blakepetersen.io dev`.
    2. Open `http://localhost:3000/` in a browser.
    3. Default theme is dark (per layout.tsx). Verify DARK MODE first:
       - All 5 sections render in the order dx_workbench → stack → collections → recent_posts → contribute.
       - Stack chips render as `Badge variant="outline"` (outlined, muted text).
       - Bracket CTAs `[skills] [hooks] [configs] [guides]` are primary/amber and underline on hover.
       - Shell CTAs `$ report-problem`, `$ suggest-improvement` behave the same.
       - CategoryCard grid renders recent collections with `hover:border-primary`.
       - Recent posts list (if any posts exist) shows title + muted date.
       - No console hydration warnings, no layout shift on load.
    4. Click the theme toggle (header). Verify LIGHT MODE:
       - Same layout, no visible artifacts.
       - Accent amber still reserved to CTAs + hover states only (no stray amber on body text).
       - Contrast is adequate — muted text remains legible, borders visible.
    5. Toggle dark → light → dark → light several times:
       - No flicker of wrong theme.
       - No hydration mismatch in console.
       - No visible layout shift.
    6. Resize to 375px (mobile), 768px (tablet), 1280px (desktop):
       - Single column at 375.
       - 2-column collection grid at 768 (or as Pencil dictates).
       - Max layout width at 1280 with `max-w-[1600px]` container.
    7. Keyboard navigation — Tab through: focus-visible ring (`ring-ring`) visible on every interactive element (CTAs, bracket links, category cards).
    8. Compare side-by-side against Pencil Homepage frame via `mcp__pencil__get_screenshot Homepage`:
       - Section order matches.
       - Typography roles match (H1 editorial, section captions muted, body mono).
       - No stray color literals visible.

    If ANY of the above fails, describe the issue and do not resume. If ALL pass, type "approved".
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| server (SSG)→HTML | `page.tsx` is a server component; data fetched at build time from Velite JSON. No runtime user input crosses into this render path. |
| client-island (Modal)→DOM | Only present if Pencil showed a Modal. Island isolates interactivity; no external data flows in. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-26-02-01 | Tampering | Client-boundary creep converts `page.tsx` to `'use client'` | mitigate | Modal (if introduced) wraps in `home-subscribe-modal.tsx` island; acceptance criterion asserts `page.tsx` does NOT start with `'use client'`. Build failure if `getCollection` runs at request time. |
| T-26-02-02 | Information Disclosure | Open-redirect via CTA `href` | accept | All homepage CTAs are hardcoded internal paths (`/skills`, `/hooks`, `/configs`, `/guides`) or known external GitHub URLs. No user-controlled href entering the render. |
| T-26-02-03 | Tampering (theme artifacts) | Hardcoded color literal regression | mitigate | Acceptance criterion greps for `bg-(amber\|cyan\|emerald\|red\|zinc)-[0-9]` and requires empty result. Phase 25 audit stays canonical. |
</threat_model>

<verification>
1. `pnpm --filter blakepetersen.io typecheck` exits 0.
2. `pnpm --filter blakepetersen.io build` succeeds; stderr shows no hydration warnings.
3. Task 2 checkpoint approved (light + dark smoke check passes per D-07).
4. No hardcoded color literals added (grep guard empty).
5. Data contract preserved: `getAllCollections` and `getCollection('posts')` still present in `page.tsx`.
</verification>

<success_criteria>
- Homepage visually matches the Pencil frame in both light and dark modes.
- Badge primitive replaces inline stack-chip spans.
- `page.tsx` remains a server component.
- Build + typecheck clean, zero hydration warnings.
- D-07 smoke check approved by Blake.
</success_criteria>

<output>
After completion, create `.planning/phases/26-blakepetersen-io-page-updates/26-02-SUMMARY.md` documenting:
- Final section composition (with any Pencil-driven deviations from UI-SPEC defaults)
- Whether the Modal subscribe island was introduced and why
- Smoke-check observations (any theme-mode quirks observed and resolved)
</output>
