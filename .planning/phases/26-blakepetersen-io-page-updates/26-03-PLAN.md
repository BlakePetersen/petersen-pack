---
phase: 26-blakepetersen-io-page-updates
plan: 03
type: execute
wave: 2
depends_on: [01]
autonomous: false
requirements: [SITE-04]
requirements_addressed: [SITE-04]
tags: [bp.io, skills-detail, site-04, prev-next-nav]
files_modified:
  - apps/blakepetersen.io/src/components/dx-content-layout.tsx
  - apps/blakepetersen.io/src/components/post-layout.tsx
  - apps/blakepetersen.io/src/components/page-navigation.tsx
must_haves:
  truths:
    - "Skills Detail pages render MDX via the existing createCollectionDetailPage factory (preserved)"
    - "PrevNextNav (from artax-ui) replaces the bp.io-local PageNavigation component at the foot of skills/configs/hooks/guides/posts detail pages"
    - "Prev/next slug resolution (buildNavData + getPrevNext) lifts into the layout consumers (dx-content-layout.tsx and post-layout.tsx); the primitive is dumb"
    - "apps/blakepetersen.io/src/components/page-navigation.tsx is deleted after consumers swap"
    - "Skills Detail renders cleanly in both light and dark modes per D-07"
  artifacts:
    - path: "apps/blakepetersen.io/src/components/dx-content-layout.tsx"
      provides: "DxContentLayout rendering MDX body + PrevNextNav footer"
      contains: "PrevNextNav"
    - path: "apps/blakepetersen.io/src/components/post-layout.tsx"
      provides: "PostLayout rendering MDX body + PrevNextNav footer"
      contains: "PrevNextNav"
  key_links:
    - from: "apps/blakepetersen.io/src/components/dx-content-layout.tsx"
      to: "artax-ui PrevNextNav"
      via: "import { PrevNextNav } from 'artax-ui'"
      pattern: "PrevNextNav"
    - from: "apps/blakepetersen.io/src/components/dx-content-layout.tsx"
      to: "apps/blakepetersen.io/src/lib/navigation.ts"
      via: "buildNavData + getPrevNext resolution (lifted from page-navigation.tsx)"
      pattern: "buildNavData|getPrevNext"
---

<objective>
Retire `apps/blakepetersen.io/src/components/page-navigation.tsx` by lifting its slug-resolution logic (`buildNavData + getPrevNext`) into the layout consumers (`dx-content-layout.tsx`, `post-layout.tsx`) and rendering the new artax-ui `PrevNextNav` primitive. Surgically recompose Skills Detail layout (header, metadata, MDX body) per the Pencil frame.

Purpose: Delivers SITE-04.
Pre-condition: Plan 01 shipped (PrevNextNav in artax-ui). Pencil desktop app running for `mcp__pencil__get_screenshot "Skills Detail Page"`.

**MDX primitive registration scope (RESOLVED):** Per RESEARCH Open Q #2 RESOLVED, grep against `apps/blakepetersen.io/content/` for `<AuthorNote` and `<DecisionRationale` returned zero matches. Neither primitive is currently used inline in MDX. The MDX pipeline (`mdx-content.tsx`) stays untouched by this plan. The canonical AuthorNote re-export on `mdxComponents.AuthorNote` is handled by Plan 01b independently (Pitfall 1 collision fix) — that does not require this plan to touch `mdx-content.tsx`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/26-blakepetersen-io-page-updates/26-CONTEXT.md
@.planning/phases/26-blakepetersen-io-page-updates/26-UI-SPEC.md
@.planning/phases/26-blakepetersen-io-page-updates/26-RESEARCH.md
@.planning/phases/26-blakepetersen-io-page-updates/26-PATTERNS.md
@.planning/phases/26-blakepetersen-io-page-updates/26-01-SUMMARY.md

<interfaces>
From artax-ui barrel (after Plan 01):
```ts
import { PrevNextNav } from 'artax-ui'  // { prev?: {href, label}, next?: {href, label}, className? }
```

From apps/blakepetersen.io/src/lib/navigation.ts (existing, tested):
```ts
buildNavData(collection): NavData
getPrevNext(navData, currentSlug): { prev?: {slug, title}, next?: {slug, title} }
```

Existing bp.io page-navigation.tsx (to be deleted) shape — current call site pattern:
```tsx
<PageNavigation slug={item.slug} />   // internally resolves prev/next
```
New call site pattern:
```tsx
const prev = prevItem && { href: `/${collection.slug}/${prevItem.slug}`, label: prevItem.title }
const next = nextItem && { href: `/${collection.slug}/${nextItem.slug}`, label: nextItem.title }
<PrevNextNav prev={prev} next={next} />
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Lift resolution into layouts, swap to PrevNextNav, delete page-navigation.tsx, recompose Skills Detail header</name>
  <files>apps/blakepetersen.io/src/components/dx-content-layout.tsx, apps/blakepetersen.io/src/components/post-layout.tsx, apps/blakepetersen.io/src/components/page-navigation.tsx</files>
  <read_first>
    - apps/blakepetersen.io/src/components/page-navigation.tsx (full — 54 lines) — extract `buildNavData + getPrevNext` logic into consumers
    - apps/blakepetersen.io/src/components/dx-content-layout.tsx (full) — line 8 import, line ~96 usage of PageNavigation
    - apps/blakepetersen.io/src/components/post-layout.tsx (full) — line 7 import, line ~55 usage of PageNavigation
    - apps/blakepetersen.io/src/lib/navigation.ts — `buildNavData` + `getPrevNext` public API
    - apps/blakepetersen.io/src/lib/collection-pages.tsx — factory that invokes the layouts
    - .planning/phases/26-blakepetersen-io-page-updates/26-PATTERNS.md §"dx-content-layout.tsx" and §"post-layout.tsx"
    - .planning/phases/26-blakepetersen-io-page-updates/26-UI-SPEC.md §"Skills Detail" — header composition, reading column width `max-w-[72ch]`
    - .planning/phases/26-blakepetersen-io-page-updates/26-01-SUMMARY.md — confirm PrevNextNav import path from Plan 01
  </read_first>
  <action>
    **Scope note:** Per RESEARCH Open Q #2 RESOLVED, no MDX content uses `<AuthorNote>` or `<DecisionRationale>` today. This plan does NOT touch `mdx-content.tsx` or register anything in the MDX components map. If a future content author introduces inline usage, a follow-up plan adds the registration — but that work is out of scope here.

    **Confirm swap surface up front** (one grep, record in SUMMARY.md):
    ```bash
    grep -rn "PageNavigation\|page-navigation" apps/blakepetersen.io/src/
    ```
    Expected: references only in `dx-content-layout.tsx` and `post-layout.tsx`. If any other file imports `PageNavigation`, add it to the swap plan.

    **Step 1: Lift resolution into `dx-content-layout.tsx`.**

    In `apps/blakepetersen.io/src/components/dx-content-layout.tsx`:
    - Replace `import { PageNavigation } from './page-navigation'` with `import { PrevNextNav } from 'artax-ui'`.
    - Add imports from `../lib/navigation` for whatever public helpers `page-navigation.tsx` uses (read that file to confirm — likely `buildNavData` and `getPrevNext`).
    - At the layout body, where `<PageNavigation slug={item.slug} />` was rendered, resolve `prev`/`next` from the collection's nav data and the current slug, then pass to `PrevNextNav`:

    ```tsx
    const navData = buildNavData(collectionItems)  // or equivalent — lift signature from page-navigation.tsx
    const { prev, next } = getPrevNext(navData, item.slug)
    const prevSlot = prev && { href: `/${collection.slug}/${prev.slug}`, label: prev.title }
    const nextSlot = next && { href: `/${collection.slug}/${next.slug}`, label: next.title }
    // ...
    <PrevNextNav prev={prevSlot} next={nextSlot} />
    ```

    Use the actual collection/item variable names present in the layout; the above is a shape guide. Preserve everything else: breadcrumbs, sidebar nav, `max-w-[72ch]` reading column (per UI-SPEC line 221), `<MDXContent />` invocation, Pencil-frame-aligned header (title `text-3xl font-mono-alt`, meta in `text-muted-foreground text-xs`).

    **Step 2: Same swap in `post-layout.tsx`.**

    Identical swap pattern for `apps/blakepetersen.io/src/components/post-layout.tsx` — replace PageNavigation import + usage with PrevNextNav + lifted resolution. Post layout likely uses a different container width (posts are typically `max-w-prose`). Do not change that width unless Pencil dictates.

    **Step 3: Delete `page-navigation.tsx` AFTER both consumers swapped.**

    Run the swap and ensure typecheck passes with the new code. Only then:
    ```bash
    rm apps/blakepetersen.io/src/components/page-navigation.tsx
    ```
    Confirm no remaining imports:
    ```bash
    grep -rn "page-navigation" apps/blakepetersen.io/src/
    ```
    Should return empty.

    **Step 4: Pencil-driven Skills Detail header composition.**

    Use `mcp__pencil__get_screenshot "Skills Detail Page"` to drive the metadata header shape. Per UI-SPEC:
    - Breadcrumbs (keep — already in layout).
    - Skill title as H1 `text-3xl font-mono-alt leading-tight`.
    - Metadata (`applies_to`, date) as `text-muted-foreground text-xs font-mono`.
    - `// section_name` section captions if Pencil shows them.
    - Body column `max-w-[72ch]`.

    **Regression guards:**
    - `grep -E 'bg-(amber|cyan|emerald|red|zinc)-[0-9]' apps/blakepetersen.io/src/components/dx-content-layout.tsx apps/blakepetersen.io/src/components/post-layout.tsx` returns empty.
    - `grep -rn "page-navigation\|PageNavigation" apps/blakepetersen.io/src/` returns empty after delete.
    - `pnpm --filter blakepetersen.io test -- --watchAll=false --testPathPattern=navigation` still passes (existing `tests/navigation.test.ts` covers `buildNavData` + `getPrevNext`).
  </action>
  <verify>
    <automated>pnpm --filter blakepetersen.io typecheck && pnpm --filter blakepetersen.io test -- --watchAll=false && pnpm --filter blakepetersen.io build</automated>
  </verify>
  <acceptance_criteria>
    - `apps/blakepetersen.io/src/components/dx-content-layout.tsx` contains `import { PrevNextNav } from 'artax-ui'`
    - `apps/blakepetersen.io/src/components/dx-content-layout.tsx` does NOT contain `PageNavigation` (old import removed)
    - `apps/blakepetersen.io/src/components/post-layout.tsx` contains `import { PrevNextNav } from 'artax-ui'`
    - `apps/blakepetersen.io/src/components/post-layout.tsx` does NOT contain `PageNavigation`
    - `apps/blakepetersen.io/src/components/dx-content-layout.tsx` contains a reference to `buildNavData` OR `getPrevNext` (resolution lifted from deleted file)
    - `apps/blakepetersen.io/src/components/page-navigation.tsx` DOES NOT EXIST
    - `grep -rn "page-navigation\|PageNavigation" apps/blakepetersen.io/src/` returns empty
    - `apps/blakepetersen.io/src/components/mdx-content.tsx` is NOT modified by this plan (per RESOLVED Open Q #2)
    - `grep -E 'bg-(amber|cyan|emerald|red|zinc)-[0-9]' apps/blakepetersen.io/src/components/dx-content-layout.tsx apps/blakepetersen.io/src/components/post-layout.tsx` returns empty
    - `pnpm --filter blakepetersen.io typecheck` exits 0
    - `pnpm --filter blakepetersen.io test -- --watchAll=false` exits 0 (navigation tests still pass)
    - `pnpm --filter blakepetersen.io build` succeeds with no hydration warnings
  </acceptance_criteria>
  <done>PrevNextNav primitive wired into both content layouts; slug resolution lifted from deleted page-navigation.tsx; Skills Detail header matches Pencil; MDX pipeline untouched per RESOLVED Open Q #2; full build clean.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: D-07 light/dark smoke check (Skills Detail + all collection detail routes)</name>
  <what-built>PrevNextNav swap across both detail layouts (dx-content-layout, post-layout); Skills Detail Pencil-matched header; deleted bp.io-local page-navigation.tsx. MDX pipeline untouched.</what-built>
  <how-to-verify>
    1. `pnpm --filter blakepetersen.io dev`.
    2. Pick a representative skill slug from `apps/blakepetersen.io/content/skills/`. Open `http://localhost:3000/skills/<slug>`.
    3. Verify DARK MODE (default):
       - Breadcrumbs at top (existing, preserved).
       - Skill title as H1, editorial font (`font-mono-alt`), `text-3xl`.
       - Metadata (`applies_to`, date) in `text-muted-foreground text-xs`.
       - Reading column centered, `max-w-[72ch]` width.
       - MDX body renders: code blocks with shiki theme-aware highlighting, headings, paragraphs — no regressions.
       - At the foot: `← prev: <title>` / `next: <title> →` symmetric, mono. Arrows are `←` / `→`, NOT `<` / `>`.
       - `hover:text-primary` + `hover:underline` on prev/next links.
       - If the skill is first in collection: only `next` appears (no prev); if last, only prev.
    4. Click through prev/next to confirm navigation works.
    5. Toggle to LIGHT MODE. Same checks pass; accent amber still reserved; borders + muted text legible.
    6. Toggle repeatedly — no flicker, no hydration mismatch in console.
    7. Also sanity-check one POST detail route (e.g., `/posts/<slug>`) — same PrevNextNav shape should render, post-layout.tsx swap verified.
    8. Responsive: 375px / 768px / 1280px — single column / sidebar inlines / max width.

    Type "approved" if all checks pass.
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| MDX (compiled)→render | Velite compiles authored MDX at build; no runtime user input. PrevNextNav receives only internal slugs resolved from the registry. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-26-03-01 | Information Disclosure | Open-redirect via prev/next slot href | mitigate | hrefs constructed as `/${collection.slug}/${item.slug}` from registry-owned data (Velite-compiled). No user input crosses this boundary. |
| T-26-03-02 | Elevation | Deleting page-navigation.tsx before all consumers swap | mitigate | Delete is Step 3, AFTER both layout swaps + typecheck pass. Grep guard in acceptance criteria ensures zero references remain. |
| T-26-03-03 | Tampering | Color-literal regression | mitigate | Grep guard in acceptance criteria. |
</threat_model>

<verification>
1. `pnpm --filter blakepetersen.io typecheck` exits 0.
2. `pnpm --filter blakepetersen.io test` exits 0 (navigation tests green).
3. `pnpm --filter blakepetersen.io build` succeeds with zero hydration warnings.
4. `page-navigation.tsx` deleted; no remaining references.
5. `mdx-content.tsx` unmodified (RESOLVED Open Q #2).
6. Task 2 smoke check approved (light + dark).
</verification>

<success_criteria>
- Skills Detail + all detail routes visually match Pencil in both themes.
- PrevNextNav replaces PageNavigation everywhere.
- Resolution logic lifted cleanly; existing navigation tests still pass.
- MDX pipeline untouched (RESOLVED Open Q #2).
- D-07 approved.
</success_criteria>

<output>
After completion, create `.planning/phases/26-blakepetersen-io-page-updates/26-03-SUMMARY.md` documenting:
- Pre-swap grep confirmation (which files imported PageNavigation)
- Any resolution-lift surprises (if `buildNavData + getPrevNext` needed extending)
- The final delete confirmation for `page-navigation.tsx`
- Smoke-check observations
</output>
