---
phase: 07-site-discovery
plan: 03
type: execute
wave: 2
depends_on: ["07-01"]
files_modified:
  - apps/blakepetersen.io/src/lib/og-image.tsx
  - apps/blakepetersen.io/assets/JetBrainsMono-Bold.ttf
  - apps/blakepetersen.io/src/app/skills/opengraph-image.tsx
  - apps/blakepetersen.io/src/app/skills/[...slug]/opengraph-image.tsx
  - apps/blakepetersen.io/src/app/hooks/opengraph-image.tsx
  - apps/blakepetersen.io/src/app/hooks/[...slug]/opengraph-image.tsx
  - apps/blakepetersen.io/src/app/configs/opengraph-image.tsx
  - apps/blakepetersen.io/src/app/configs/[...slug]/opengraph-image.tsx
  - apps/blakepetersen.io/src/app/guides/opengraph-image.tsx
  - apps/blakepetersen.io/src/app/guides/[...slug]/opengraph-image.tsx
  - apps/blakepetersen.io/src/app/posts/opengraph-image.tsx
  - apps/blakepetersen.io/src/app/posts/[...slug]/opengraph-image.tsx
autonomous: true
requirements:
  - SITE-05

must_haves:
  truths:
    - "Every page has a unique OG image with terminal aesthetic"
    - "Detail page OG images show title + category label"
    - "Listing page OG images show category name + item count"
  artifacts:
    - path: "apps/blakepetersen.io/src/lib/og-image.tsx"
      provides: "Shared OG image renderer function"
      exports: ["renderOgImage"]
    - path: "apps/blakepetersen.io/src/app/skills/[...slug]/opengraph-image.tsx"
      provides: "Detail page OG image for skills"
      exports: ["default", "alt", "size", "contentType"]
    - path: "apps/blakepetersen.io/src/app/skills/opengraph-image.tsx"
      provides: "Listing page OG image for skills"
      exports: ["default", "alt", "size", "contentType"]
    - path: "apps/blakepetersen.io/assets/JetBrainsMono-Bold.ttf"
      provides: "Font file for OG image rendering"
  key_links:
    - from: "apps/blakepetersen.io/src/app/skills/[...slug]/opengraph-image.tsx"
      to: "src/lib/og-image.tsx"
      via: "shared renderOgImage function"
      pattern: "renderOgImage"
    - from: "apps/blakepetersen.io/src/app/skills/opengraph-image.tsx"
      to: "src/lib/og-image.tsx"
      via: "shared renderOgImage function"
      pattern: "renderOgImage"
    - from: "apps/blakepetersen.io/src/app/hooks/opengraph-image.tsx"
      to: "src/lib/og-image.tsx"
      via: "shared renderOgImage (same pattern as skills)"
      pattern: "renderOgImage"
    - from: "apps/blakepetersen.io/src/app/hooks/[...slug]/opengraph-image.tsx"
      to: "src/lib/og-image.tsx"
      via: "shared renderOgImage (same pattern as skills)"
      pattern: "renderOgImage"
---

<objective>
Add terminal-styled OG images to all 10 content routes (5 listing + 5 detail pages).

Purpose: Every page gets a unique Open Graph image for social sharing, rendered at build time with terminal aesthetic.
Output: Shared renderOgImage function and 10 opengraph-image.tsx route files.
</objective>

<execution_context>
@/Users/blake/.claude/get-shit-done/workflows/execute-plan.md
@/Users/blake/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-site-discovery/07-RESEARCH.md
@.planning/phases/07-site-discovery/07-01-SUMMARY.md

@apps/blakepetersen.io/src/app/skills/[...slug]/page.tsx

<interfaces>
From src/lib/content.ts:
```typescript
export function getSkills(): DxItem[]
export function getHooks(): DxItem[]
export function getConfigs(): DxItem[]
export function getGuides(): DxItem[]
export function getPosts(): PostItem[]
```

Note: All 10 opengraph-image.tsx files are thin wrappers (~10 lines each) calling the shared renderOgImage function. The real logic lives in src/lib/og-image.tsx.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Shared OG image renderer and font asset</name>
  <files>
    apps/blakepetersen.io/src/lib/og-image.tsx,
    apps/blakepetersen.io/assets/JetBrainsMono-Bold.ttf
  </files>
  <action>
    1. Download JetBrains Mono Bold TTF to apps/blakepetersen.io/assets/JetBrainsMono-Bold.ttf
       - Use: `curl -L -o assets/JetBrainsMono-Bold.ttf "https://github.com/JetBrains/JetBrainsMono/raw/master/fonts/ttf/JetBrainsMono-Bold.ttf"`

    2. Create src/lib/og-image.tsx with shared rendering function:
       - `renderOgImage({ title, category, itemCount? })` — returns ImageResponse
       - Loads JetBrains Mono Bold from assets/ via readFile(join(process.cwd(), 'assets/...'))
       - Terminal aesthetic: dark background (#0a0a0a), monospace font, category label in amber (#f59e0b)
       - Layout: "// blake_petersen" at top, category label, title text, terminal-style border/decoration
       - Size: 1200x630, contentType: image/png
       - For listing pages: show category name + item count (e.g., "12 skills")
       - For detail pages: show title + category label
       - IMPORTANT: Satori only supports inline styles, display:flex, flexDirection. No CSS grid, no CSS variables, no gap in all contexts.
       - ABOUTME comments
  </action>
  <verify>
    <automated>cd apps/blakepetersen.io && test -f assets/JetBrainsMono-Bold.ttf && test -f src/lib/og-image.tsx && node -e "require('./src/lib/og-image.tsx')" 2>&1 | head -5; echo "FILES_EXIST"</automated>
  </verify>
  <done>Shared OG image renderer created with terminal aesthetic. Font asset downloaded. renderOgImage function accepts title, category, and optional itemCount.</done>
</task>

<task type="auto">
  <name>Task 2: OG image route files for all 10 content routes</name>
  <files>
    apps/blakepetersen.io/src/app/skills/opengraph-image.tsx,
    apps/blakepetersen.io/src/app/skills/[...slug]/opengraph-image.tsx,
    apps/blakepetersen.io/src/app/hooks/opengraph-image.tsx,
    apps/blakepetersen.io/src/app/hooks/[...slug]/opengraph-image.tsx,
    apps/blakepetersen.io/src/app/configs/opengraph-image.tsx,
    apps/blakepetersen.io/src/app/configs/[...slug]/opengraph-image.tsx,
    apps/blakepetersen.io/src/app/guides/opengraph-image.tsx,
    apps/blakepetersen.io/src/app/guides/[...slug]/opengraph-image.tsx,
    apps/blakepetersen.io/src/app/posts/opengraph-image.tsx,
    apps/blakepetersen.io/src/app/posts/[...slug]/opengraph-image.tsx
  </files>
  <action>
    1. Create opengraph-image.tsx for each of the 5 listing routes:
       - Export alt, size (1200x630), contentType ('image/png')
       - Export default function that calls renderOgImage with category name and item count from collection getter
       - Each is ~10 lines calling the shared function
       - ABOUTME comments

    2. Create opengraph-image.tsx for each of the 5 detail routes ([...slug]/):
       - Export alt, size, contentType
       - Export default async function that receives params (Promise<{ slug: string[] }>), looks up item, calls renderOgImage with title and category
       - Falls back to collection name if item not found
       - ABOUTME comments

    All 10 files are thin wrappers around renderOgImage — they differ only in which collection getter they call and the category name string.
  </action>
  <verify>
    <automated>cd apps/blakepetersen.io && pnpm build 2>&1 | tail -20</automated>
  </verify>
  <done>Every content route has a unique OG image generated at build time. Terminal aesthetic with dark background, monospace font, amber accent. Detail pages show title + category, listing pages show category + count.</done>
</task>

</tasks>

<verification>
- `pnpm --filter blakepetersen.io build` — builds successfully with OG images generated
- All 10 opengraph-image.tsx files exist and export default + alt + size + contentType
</verification>

<success_criteria>
- All 10 routes have opengraph-image.tsx generating terminal-styled images
- OG images use JetBrains Mono font with dark background and amber accent
- Detail pages show title + category, listing pages show category + count
</success_criteria>

<output>
After completion, create `.planning/phases/07-site-discovery/07-03-SUMMARY.md`
</output>
