---
phase: 07-site-discovery
plan: 02
type: execute
wave: 2
depends_on: ["07-01"]
files_modified:
  - apps/blakepetersen.io/src/app/skills/page.tsx
  - apps/blakepetersen.io/src/app/skills/[...slug]/page.tsx
  - apps/blakepetersen.io/src/app/hooks/page.tsx
  - apps/blakepetersen.io/src/app/hooks/[...slug]/page.tsx
  - apps/blakepetersen.io/src/app/configs/page.tsx
  - apps/blakepetersen.io/src/app/configs/[...slug]/page.tsx
  - apps/blakepetersen.io/src/app/guides/page.tsx
  - apps/blakepetersen.io/src/app/guides/[...slug]/page.tsx
  - apps/blakepetersen.io/src/app/posts/page.tsx
  - apps/blakepetersen.io/src/app/posts/[...slug]/page.tsx
  - apps/blakepetersen.io/tests/metadata.test.ts
  - apps/blakepetersen.io/tests/structured-data.test.ts
autonomous: true
requirements:
  - SITE-05

must_haves:
  truths:
    - "Every detail page has unique title, description, and canonical URL in metadata"
    - "Every detail page has TechArticle JSON-LD structured data"
    - "Every detail page has BreadcrumbList JSON-LD structured data"
    - "Every listing page has descriptive title and description with item count"
  artifacts:
    - path: "apps/blakepetersen.io/src/app/skills/[...slug]/page.tsx"
      provides: "generateMetadata + JSON-LD for skills detail"
      contains: "generateMetadata"
    - path: "apps/blakepetersen.io/src/app/skills/page.tsx"
      provides: "generateMetadata for skills listing"
      contains: "generateMetadata"
    - path: "apps/blakepetersen.io/tests/metadata.test.ts"
      provides: "Unit tests for buildMetadata helper"
    - path: "apps/blakepetersen.io/tests/structured-data.test.ts"
      provides: "Unit tests for JSON-LD generation"
  key_links:
    - from: "apps/blakepetersen.io/src/app/skills/[...slug]/page.tsx"
      to: "src/lib/metadata.ts"
      via: "generateMetadata export using buildMetadata"
      pattern: "generateMetadata"
    - from: "apps/blakepetersen.io/src/app/skills/[...slug]/page.tsx"
      to: "src/components/json-ld.tsx"
      via: "JsonLd component in page render"
      pattern: "JsonLd"
    - from: "apps/blakepetersen.io/src/app/hooks/[...slug]/page.tsx"
      to: "src/lib/metadata.ts"
      via: "generateMetadata export using buildMetadata (same pattern as skills)"
      pattern: "generateMetadata"
    - from: "apps/blakepetersen.io/src/app/configs/[...slug]/page.tsx"
      to: "src/lib/metadata.ts"
      via: "generateMetadata export using buildMetadata (same pattern as skills)"
      pattern: "generateMetadata"
    - from: "apps/blakepetersen.io/src/app/guides/[...slug]/page.tsx"
      to: "src/lib/metadata.ts"
      via: "generateMetadata export using buildMetadata (same pattern as skills)"
      pattern: "generateMetadata"
    - from: "apps/blakepetersen.io/src/app/posts/[...slug]/page.tsx"
      to: "src/lib/metadata.ts"
      via: "generateMetadata export using buildMetadata (same pattern as skills)"
      pattern: "generateMetadata"
---

<objective>
Add generateMetadata and JSON-LD structured data to all 10 content routes (5 listing + 5 detail pages).

Purpose: Every page gets SEO-optimized metadata and structured data for search engines.
Output: All routes have generateMetadata exports and JSON-LD script tags.
</objective>

<execution_context>
@/Users/blake/.claude/get-shit-done/workflows/execute-plan.md
@/Users/blake/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-site-discovery/07-RESEARCH.md
@.planning/phases/07-site-discovery/07-01-SUMMARY.md

@apps/blakepetersen.io/src/app/skills/page.tsx
@apps/blakepetersen.io/src/app/skills/[...slug]/page.tsx
@apps/blakepetersen.io/src/components/breadcrumbs.tsx

<interfaces>
<!-- From Plan 01 outputs -->

From src/lib/metadata.ts (created in Plan 01):
```typescript
export function buildMetadata(item: { title: string; seo_title?: string; description: string; seo_description?: string; excerpt: string; slug: string }, collection: string): Metadata
export function buildArticleJsonLd(item: { title: string; description: string; date?: string; slug: string }, collection: string): WithContext<Article>
export function buildBreadcrumbJsonLd(pathname: string): WithContext<BreadcrumbList>
export function escapeXml(str: string): string
```

From src/components/json-ld.tsx (created in Plan 01):
```typescript
export function JsonLd({ data }: { data: WithContext<Thing> }): JSX.Element
```

From src/lib/content.ts:
```typescript
export function getSkills(): DxItem[]
export function getHooks(): DxItem[]
// ... etc for all collections
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: generateMetadata and JSON-LD on all routes</name>
  <files>
    apps/blakepetersen.io/src/app/skills/page.tsx,
    apps/blakepetersen.io/src/app/skills/[...slug]/page.tsx,
    apps/blakepetersen.io/src/app/hooks/page.tsx,
    apps/blakepetersen.io/src/app/hooks/[...slug]/page.tsx,
    apps/blakepetersen.io/src/app/configs/page.tsx,
    apps/blakepetersen.io/src/app/configs/[...slug]/page.tsx,
    apps/blakepetersen.io/src/app/guides/page.tsx,
    apps/blakepetersen.io/src/app/guides/[...slug]/page.tsx,
    apps/blakepetersen.io/src/app/posts/page.tsx,
    apps/blakepetersen.io/src/app/posts/[...slug]/page.tsx,
    apps/blakepetersen.io/tests/metadata.test.ts,
    apps/blakepetersen.io/tests/structured-data.test.ts
  </files>
  <behavior>
    - Test: buildMetadata returns seo_title when present, falls back to title
    - Test: buildMetadata returns seo_description when present, falls back to excerpt
    - Test: buildMetadata includes canonical URL with collection prefix
    - Test: buildArticleJsonLd returns TechArticle with correct headline, author, datePublished
    - Test: buildBreadcrumbJsonLd returns BreadcrumbList with correct items from pathname
  </behavior>
  <action>
    1. Add `generateMetadata` export to all 5 detail pages ([...slug]/page.tsx):
       - Use buildMetadata from lib/metadata.ts with the found content item
       - Pattern: look up item by slug, return buildMetadata(item, 'skills') etc.
       - For listing pages: return static metadata with descriptive title and description including item count
         - Example: title "Skills", description "Browse {count} Claude Code skills for AI-first development"

    2. Add JSON-LD structured data to all 5 detail page components:
       - Import JsonLd from components/json-ld.tsx
       - Render two JsonLd components: one for TechArticle (via buildArticleJsonLd), one for BreadcrumbList (via buildBreadcrumbJsonLd)
       - Place before the existing DxContentLayout/PostLayout render

    3. Add `generateMetadata` to all 5 listing pages (page.tsx):
       - Return Metadata with title (collection name), description (count + type summary)
       - Canonical URL for the listing path

    All 5 content types follow the same pattern. Skills is the template — hooks, configs, guides, posts follow identically with their respective getter and collection name. Posts use PostLayout and have date field; DX types use DxContentLayout.
  </action>
  <verify>
    <automated>cd apps/blakepetersen.io && pnpm test -- --testPathPattern "metadata|structured-data"</automated>
  </verify>
  <done>All 10 route pages export generateMetadata. All 5 detail pages render TechArticle + BreadcrumbList JSON-LD. Listing pages show item count in description.</done>
</task>

</tasks>

<verification>
- `pnpm --filter blakepetersen.io test` — all metadata and structured data tests pass
- `pnpm --filter blakepetersen.io build` — builds successfully
</verification>

<success_criteria>
- All 10 content route pages export generateMetadata with correct title/description/canonical
- All 5 detail pages render TechArticle + BreadcrumbList JSON-LD
- Listing page descriptions include item count
</success_criteria>

<output>
After completion, create `.planning/phases/07-site-discovery/07-02-SUMMARY.md`
</output>
