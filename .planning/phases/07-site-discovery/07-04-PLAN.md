---
phase: 07-site-discovery
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/blakepetersen.io/src/components/command-palette.tsx
  - apps/blakepetersen.io/src/components/search-trigger.tsx
  - apps/blakepetersen.io/src/lib/search.ts
  - apps/blakepetersen.io/src/components/header.tsx
  - apps/blakepetersen.io/package.json
  - apps/blakepetersen.io/tests/search.test.ts
  - apps/blakepetersen.io/tests/command-palette.test.tsx
autonomous: true
requirements:
  - CONT-05

must_haves:
  truths:
    - "User can press Cmd+K / Ctrl+K to open search palette"
    - "User can click search icon in header to open search palette"
    - "Search returns results from all content types grouped by type"
    - "User sees recent pages (from localStorage) when palette opens with empty query"
    - "User can navigate results with keyboard (arrow keys, Enter)"
    - "Search indexes full content (titles, metadata, and MDX body text)"
  artifacts:
    - path: "apps/blakepetersen.io/src/components/command-palette.tsx"
      provides: "Client-side command palette with search UI"
      contains: "use client"
    - path: "apps/blakepetersen.io/src/components/search-trigger.tsx"
      provides: "Search icon button for header"
      contains: "use client"
    - path: "apps/blakepetersen.io/src/lib/search.ts"
      provides: "Pagefind wrapper with lazy import and dev-mode fallback"
      exports: ["searchContent"]
    - path: "apps/blakepetersen.io/tests/search.test.ts"
      provides: "Unit tests for searchContent dev-mode fallback and result shaping"
    - path: "apps/blakepetersen.io/tests/command-palette.test.tsx"
      provides: "Render test for CommandPalette component"
  key_links:
    - from: "apps/blakepetersen.io/src/components/command-palette.tsx"
      to: "src/lib/search.ts"
      via: "imports searchContent for query execution"
      pattern: "searchContent"
    - from: "apps/blakepetersen.io/src/components/header.tsx"
      to: "src/components/command-palette.tsx"
      via: "renders CommandPalette component"
      pattern: "CommandPalette"
    - from: "apps/blakepetersen.io/src/components/header.tsx"
      to: "src/components/search-trigger.tsx"
      via: "renders SearchTrigger in nav area"
      pattern: "SearchTrigger"
---

<objective>
Add client-side full-text search with a command palette UI powered by Pagefind.

Purpose: Users can discover content across all types using Cmd+K keyboard shortcut or clicking the search icon, with results grouped by content type and keyboard navigation.
Output: Working command palette search, Pagefind build integration, search icon in header.
</objective>

<execution_context>
@/Users/blake/.claude/get-shit-done/workflows/execute-plan.md
@/Users/blake/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-site-discovery/07-RESEARCH.md

@apps/blakepetersen.io/src/components/header.tsx
@apps/blakepetersen.io/src/components/sidebar-drawer.tsx

<interfaces>
<!-- Existing patterns for client components -->

From header.tsx:
```typescript
// Header renders in a sticky bar with max-w-[1200px] container
// Nav area is on the right side: <nav><SidebarDrawer /></nav>
// Search trigger should be placed before SidebarDrawer in the nav
```

Design tokens available:
```
terminal-bg, terminal-surface, terminal-border, terminal-text, terminal-muted, amber-accent
Font: font-mono (JetBrains Mono)
```

Pagefind JS API (from research):
```typescript
// Lazy import with webpackIgnore
const pagefind = await import(/* webpackIgnore: true */ '/pagefind/pagefind.js')
const results = await pagefind.search(query)
// results.results is array of { id, data() } where data() returns Promise<{ url, title, excerpt, meta }>
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Pagefind integration and search library</name>
  <files>
    apps/blakepetersen.io/src/lib/search.ts,
    apps/blakepetersen.io/package.json,
    apps/blakepetersen.io/tests/search.test.ts
  </files>
  <behavior>
    - Test: searchContent returns empty array when Pagefind is unavailable (dev-mode fallback)
    - Test: searchContent returns shaped SearchResult[] when Pagefind resolves results
    - Test: SearchResult type has url, title, excerpt, meta fields
    - Test: searchContent limits results to top 20
  </behavior>
  <action>
    1. Install Pagefind: `pnpm --filter blakepetersen.io add -D pagefind`

    2. Add postbuild script to package.json:
       - `"postbuild": "pagefind --site .next --output-path public/pagefind"`
       - This indexes the pre-rendered HTML after `next build` completes

    3. Add `public/pagefind/` to .gitignore (generated at build time, not committed)

    4. Create src/lib/search.ts:
       - Lazy-load Pagefind with `/* webpackIgnore: true */` dynamic import from `/pagefind/pagefind.js`
       - Cache the pagefind instance in a module-level variable
       - Wrap import in try/catch — return empty array in dev mode (no index available)
       - Export `searchContent(query: string)`: calls pagefind.search(query), loads top 20 results via result.data(), returns array of SearchResult objects
       - Export type `SearchResult = { url: string; title: string; excerpt: string; meta: Record<string, string> }`
       - ABOUTME comments

    5. Create tests/search.test.ts:
       - Mock the dynamic import to test both success and failure paths
       - Test that dev-mode fallback returns empty array (import throws)
       - Test that successful search returns shaped results
       - Test that results are capped at 20
  </action>
  <verify>
    <automated>cd apps/blakepetersen.io && pnpm test -- --testPathPattern search</automated>
  </verify>
  <done>Pagefind installed as dev dependency. Postbuild script indexes rendered HTML. Search wrapper handles lazy loading with dev-mode fallback. Unit tests cover fallback and result shaping.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Command palette UI with search trigger in header</name>
  <files>
    apps/blakepetersen.io/src/components/command-palette.tsx,
    apps/blakepetersen.io/src/components/search-trigger.tsx,
    apps/blakepetersen.io/src/components/header.tsx,
    apps/blakepetersen.io/tests/command-palette.test.tsx
  </files>
  <behavior>
    - Test: CommandPalette renders without crashing
    - Test: CommandPalette opens when open prop is true
    - Test: CommandPalette shows input field when open
    - Test: SearchTrigger renders a button with accessible label
  </behavior>
  <action>
    1. Create src/components/search-trigger.tsx:
       - 'use client' component
       - Renders a button with Search icon (from lucide-react) and "Cmd+K" hint text
       - Accepts onClick prop to open the command palette
       - Styled with terminal tokens: text-terminal-muted, hover:text-amber-accent
       - On mobile, show just the icon (hide "Cmd+K" text)
       - ABOUTME comments

    2. Create src/components/command-palette.tsx:
       - 'use client' component
       - Uses Radix Dialog (DialogInteractive from artax-ui, or import @radix-ui/react-dialog directly — check what's available) for modal overlay
       - State: open/closed, query string, results array, selected index, recent pages
       - Keyboard shortcut: useEffect to listen for Cmd+K / Ctrl+K, toggles open state
       - When opened with empty query: show recent pages from localStorage (key: 'recent-pages', store last 5 visited URLs with titles)
       - On query change: debounce 200ms, call searchContent(query) from lib/search.ts
       - Group results by content type using URL prefix (/skills/*, /hooks/*, etc.) — display section headers
       - Keyboard navigation: Arrow up/down to move selection, Enter to navigate to selected result
       - On result click or Enter: navigate using window.location.href (or next/navigation router), close palette, record in recent pages
       - Recent pages tracking: on every page load, store current URL + document.title in localStorage (capped at 5)
       - Terminal aesthetic styling:
         - Dark overlay backdrop
         - Modal with terminal-bg background, terminal-border border
         - Input with monospace font, no outline, amber-accent focus ring
         - Results with hover:bg-terminal-surface, selected item highlighted with amber-accent left border
         - Group headers in terminal-muted
         - Result excerpts show Pagefind highlighted matches (Pagefind returns <mark> tags in excerpt)
       - ESC closes the palette
       - ABOUTME comments

    3. Update header.tsx:
       - Import CommandPalette and SearchTrigger
       - The simpler approach: CommandPalette is self-contained — it renders the search icon button AND the dialog. Header just renders `<CommandPalette />` in the nav. This avoids needing a shared state wrapper.

    4. Create tests/command-palette.test.tsx:
       - Mock searchContent to return empty array
       - Test that CommandPalette renders without crashing
       - Test that SearchTrigger renders with accessible label
       - Use @testing-library/react for render tests
  </action>
  <verify>
    <automated>cd apps/blakepetersen.io && pnpm test -- --testPathPattern "command-palette|search"</automated>
  </verify>
  <done>Command palette opens on Cmd+K / Ctrl+K or clicking search icon. Results are grouped by content type. Recent pages shown on empty query. Keyboard navigation works. Terminal aesthetic styling applied. Render tests pass.</done>
</task>

</tasks>

<verification>
- `pnpm --filter blakepetersen.io test -- --testPathPattern "search|command-palette"` — all search and component tests pass
- `pnpm --filter blakepetersen.io build` — builds successfully, pagefind postbuild runs and creates public/pagefind/
</verification>

<success_criteria>
- Cmd+K / Ctrl+K opens command palette from any page
- Search icon visible in header
- Full-text search returns results across all content types
- Results grouped by content type (Skills, Hooks, Configs, Guides, Posts)
- Recent pages (last 5) shown when palette opens with empty query
- Keyboard navigation between results works
- Pagefind indexes all pre-rendered HTML at build time
- Unit tests cover searchContent fallback behavior and CommandPalette rendering
</success_criteria>

<output>
After completion, create `.planning/phases/07-site-discovery/07-04-SUMMARY.md`
</output>
