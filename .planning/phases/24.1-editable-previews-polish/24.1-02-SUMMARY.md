---
phase: 24.1-editable-previews-polish
plan: 2
subsystem: ui
tags: [radix, toggle, aria-label, accessibility, tdd, semantics]

requires:
  - phase: 24-editable-previews
    provides: PlaygroundPropsForm boolean control backed by Radix Toggle
provides:
  - Radix Toggle in PlaygroundPropsForm no longer wrapped in <label>
  - aria-label={prop.name} on Toggle preserves accessible name
  - Latent double-dispatch risk from native label click-forwarding eliminated
affects: [25-theming, future-playground-work]

tech-stack:
  added: []
  patterns:
    - "Radix button primitives (Toggle) must not be <label> children — aria-label carries accessible name instead"

key-files:
  created: []
  modified:
    - apps/artax/src/components/playground-props-form.tsx
    - apps/artax/tests/playground-props-form.test.tsx

key-decisions:
  - "Boolean branch uses <div> + aria-label per 24-CONTEXT D-03 (canonical WR-02 fix)"
  - "Other branches (select/number/text) untouched — their <label> wraps real form controls"

patterns-established:
  - "Radix-backed button primitives: never wrap in <label>; attach accessible name via aria-label"

requirements-completed: [ARTAX-08]

duration: 8m
completed: 2026-04-19
---

# Phase 24.1 Plan 02: WR-02 Toggle Label Wrap Fix Summary

**Radix Toggle boolean control now renders inside a `<div>` with `aria-label={prop.name}` — eliminating native label-click forwarding as a latent double-dispatch vector**

## Performance

- **Duration:** ~8 min
- **Started:** 2026-04-19T06:02:00Z
- **Completed:** 2026-04-19T06:09:48Z
- **Tasks:** 2 (TDD: RED + GREEN)
- **Files modified:** 2

## Accomplishments

- Closed WR-02 from 24-REVIEW.md: Radix Toggle is no longer a `<label>` child
- Added `aria-label={prop.name}` on Toggle to preserve accessible name even if visible children change
- New regression test asserts `closest('label') === null` AND `toHaveBeenCalledTimes(1)` on click
- All 154 artax tests green; typecheck clean
- Select / number / text branches not touched (their `<label>` wraps real form controls — native semantics are correct there)

## Task Commits

1. **Task 1: RED — failing test** — `2e676ee` (test)
2. **Task 2: GREEN — drop `<label>`, add aria-label** — `5d169c0` (fix)

## Files Created/Modified

- `apps/artax/src/components/playground-props-form.tsx` — boolean branch `<label>` → `<div>`, Toggle gains `aria-label={prop.name}`
- `apps/artax/tests/playground-props-form.test.tsx` — added `it('fires onPressedChange exactly once per click and does not wrap Toggle in <label>', ...)`

## Before / After Diff

**Before (lines 40-53):**

```tsx
case 'boolean':
  return (
    <label
      key={prop.name}
      className="flex items-center gap-2 font-mono text-xs"
    >
      <Toggle
        pressed={current === 'true'}
        onPressedChange={(v) => update(prop.name, String(v))}
      >
        {prop.name}
      </Toggle>
    </label>
  )
```

**After:**

```tsx
case 'boolean':
  return (
    <div
      key={prop.name}
      className="flex items-center gap-2 font-mono text-xs"
    >
      <Toggle
        aria-label={prop.name}
        pressed={current === 'true'}
        onPressedChange={(v) => update(prop.name, String(v))}
      >
        {prop.name}
      </Toggle>
    </div>
  )
```

## Pre-existing Query Updates

No pre-existing test queries needed to change. Task 2 step 7 guarded against queries that used `screen.getByLabelText('disabled')` — none existed in `playground-props-form.test.tsx` or `component-playground.test.tsx`. The existing boolean-toggle test uses `screen.getByText('disabled')`, which still resolves because the prop name remains in the Toggle's visible children.

## Untouched Branches (verified)

- `case 'select':` — still wraps `<select>` in `<label>` (1 `<label>` occurrence found via grep)
- `case 'number':` — unchanged (wraps `<input type="number">`)
- `case 'text':` (default) — unchanged (wraps `<input type="text">`)

Grep assertions from plan verification all pass:
- boolean branch `<label` count: **0** ✓
- boolean branch `aria-label` count: **1** ✓
- select branch `<label` count: **1** ✓

## Decisions Made

None — executed plan verbatim per the canonical WR-02 fix from 24-REVIEW.md (D-03 in 24.1-CONTEXT).

## Deviations from Plan

None — plan executed exactly as written.

One observation (not a deviation): the RED state is deterministic via the `getByRole('button', { name: 'disabled' })` query rather than the `closest('label')` assertion. Under the pre-fix `<label>`-wrapped markup, `getByRole` cannot compute an accessible name for the button child (RTL / dom-accessibility-api does not propagate label text to button children — see Phase 24 decision note "24-04: Radix Toggle tests use getByText instead of getByRole name"). After the fix, `aria-label="disabled"` resolves the query. Net effect: RED is still deterministic; the `closest('label')` assertion is an additional structural safety net that also passes post-fix.

## Issues Encountered

- First commit attempt rejected by commitlint for `body-max-line-length` (>100 chars). Reworded; second attempt passed.

## User Setup Required

None.

## Next Phase Readiness

- WR-02 closed. Phase 24.1 plan 03 (Header hydration mismatch) remains.
- ARTAX-08 acceptance bar not regressed (154/154 tests green).

## Self-Check: PASSED

- `apps/artax/src/components/playground-props-form.tsx` — FOUND (modified)
- `apps/artax/tests/playground-props-form.test.tsx` — FOUND (modified)
- `.planning/phases/24.1-editable-previews-polish/24.1-02-SUMMARY.md` — FOUND (this file)
- Commit `2e676ee` (RED test) — FOUND in git log
- Commit `5d169c0` (GREEN fix) — FOUND in git log

---
*Phase: 24.1-editable-previews-polish*
*Completed: 2026-04-19*
