---
phase: 20-fix-integration-gaps
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/blakepetersen.io/src/components/apply-action-bar.tsx
  - apps/blakepetersen.io/src/components/dx-content-layout.tsx
  - apps/blakepetersen.io/tests/apply-action-bar.test.tsx
  - turbo.json
autonomous: true
requirements: [PKG-05, CORE-01, CORE-02]

must_haves:
  truths:
    - "ApplyActionBar renders `blink apply <slug>` with bare slug (no type prefix)"
    - "`turbo build` cache includes registry JSON files in `public/r/`"
    - "`blink list` can fetch from the live registry endpoint (or root cause is identified)"
  artifacts:
    - path: "apps/blakepetersen.io/src/components/apply-action-bar.tsx"
      provides: "ApplyActionBar component with bare slug command"
      contains: "blink apply ${slug}"
    - path: "apps/blakepetersen.io/tests/apply-action-bar.test.tsx"
      provides: "Updated test assertions for bare slug format"
      contains: "blink apply eslint-flat-config"
    - path: "turbo.json"
      provides: "Build outputs including registry files"
      contains: "public/r/**"
  key_links:
    - from: "apps/blakepetersen.io/src/components/dx-content-layout.tsx"
      to: "apps/blakepetersen.io/src/components/apply-action-bar.tsx"
      via: "slug prop (type prop removed)"
      pattern: "ApplyActionBar slug="
---

<objective>
Fix three integration gaps identified by the v1.2 milestone audit: ApplyActionBar renders wrong slug format, turbo cache misses registry files, and live registry connectivity needs verification.

Purpose: Close the final v1.2 gaps so the web-to-CLI flow works end-to-end.
Output: Working ApplyActionBar with bare slugs, correct turbo cache config, verified registry connectivity.
</objective>

<execution_context>
@/Users/blake/.claude/get-shit-done/workflows/execute-plan.md
@/Users/blake/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/20-fix-integration-gaps/20-RESEARCH.md

<interfaces>
<!-- Key types and contracts the executor needs. -->

From apps/blakepetersen.io/src/components/apply-action-bar.tsx (CURRENT — will change):
```typescript
// Current signature (type prop will be REMOVED):
export function ApplyActionBar({ type, slug }: { type: string; slug: string })
// Command currently: `blink apply ${type}/${slug}`
```

From apps/blakepetersen.io/src/components/dx-content-layout.tsx (caller — line 64):
```typescript
// Current call (type prop will be REMOVED):
{artifact && <ApplyActionBar type={artifact.type} slug={artifact.slug} />}
```

From turbo.json (current outputs):
```json
"outputs": [".next/**", "dist/**", ".velite/**"]
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Fix ApplyActionBar slug format and remove dead type prop</name>
  <files>
    apps/blakepetersen.io/src/components/apply-action-bar.tsx,
    apps/blakepetersen.io/src/components/dx-content-layout.tsx,
    apps/blakepetersen.io/tests/apply-action-bar.test.tsx
  </files>
  <behavior>
    - Test 1: ApplyActionBar renders `$ blink apply eslint-flat-config` (bare slug, no type prefix) when given slug="eslint-flat-config"
    - Test 2: ApplyActionBar renders `$ blink apply test-driven-development` (bare slug) when given slug="test-driven-development"
    - Test 3: The `$ ` prefix appears before the command
    - Test 4: A copy button is rendered
  </behavior>
  <action>
    1. Update the test file FIRST (RED phase):
       - Change ABOUTME line 2 to: "Validates that blink apply commands use bare slug format."
       - Remove the `type` prop from ALL `render(<ApplyActionBar ...>)` calls — only pass `slug`
       - Update assertion strings: `'$ blink apply config/eslint-flat-config'` becomes `'$ blink apply eslint-flat-config'`, and `'$ blink apply skill/test-driven-development'` becomes `'$ blink apply test-driven-development'`
       - Run tests — they MUST fail (component still uses type/slug format)

    2. Update the component (GREEN phase):
       - In `apply-action-bar.tsx`, change the function signature from `{ type, slug }: { type: string; slug: string }` to `{ slug }: { slug: string }`
       - Change line 9 from `const command = \`blink apply \${type}/\${slug}\`` to `const command = \`blink apply \${slug}\``
       - Run tests — they MUST pass

    3. Update the caller:
       - In `dx-content-layout.tsx` line 64, change `<ApplyActionBar type={artifact.type} slug={artifact.slug} />` to `<ApplyActionBar slug={artifact.slug} />`
       - If the `artifact.type` destructure is no longer used anywhere else in the file, leave it (it may be used elsewhere in the layout)

    4. Run `pnpm --filter blakepetersen.io test -- --testPathPattern apply-action-bar` — all 4 tests pass
  </action>
  <verify>
    <automated>cd /Users/blake/Sites/petersen-group && pnpm --filter blakepetersen.io test -- --testPathPattern apply-action-bar</automated>
  </verify>
  <done>ApplyActionBar renders `blink apply <slug>` with bare slug. Type prop removed from component and caller. All 4 tests pass with updated assertions.</done>
</task>

<task type="auto">
  <name>Task 2: Add registry outputs to turbo.json and verify live registry</name>
  <files>turbo.json</files>
  <action>
    1. In `turbo.json`, add `"public/r/**"` to the `build.outputs` array so it becomes:
       ```json
       "outputs": [".next/**", "dist/**", ".velite/**", "public/r/**"]
       ```

    2. Verify live registry connectivity by running:
       ```bash
       curl -s -o /dev/null -w "%{http_code}" https://blakepetersen.io/r/index.json
       ```
       - If 200: registry is live, CORE-02 is satisfied. Log the result.
       - If 404: the registry files are not deployed yet. This is expected if the site hasn't been redeployed since Phase 14. Document in SUMMARY that registry connectivity requires a fresh deployment with `public/r/**` in the build outputs. This is NOT a code bug — it's a deployment sequencing issue.
       - If other error: document the error and response headers in SUMMARY for follow-up.

    3. Run `pnpm --filter blakepetersen.io test` to confirm no regressions from task 1.
  </action>
  <verify>
    <automated>cd /Users/blake/Sites/petersen-group && node -e "const t = require('./turbo.json'); const ok = t.tasks.build.outputs.includes('public/r/**'); console.log(ok ? 'PASS: public/r/** in outputs' : 'FAIL: missing public/r/**'); process.exit(ok ? 0 : 1)"</automated>
  </verify>
  <done>turbo.json build outputs include `public/r/**`. Live registry connectivity checked and result documented.</done>
</task>

</tasks>

<verification>
1. `pnpm --filter blakepetersen.io test -- --testPathPattern apply-action-bar` — all 4 tests pass with bare slug format
2. `pnpm --filter blakepetersen.io test` — full app test suite green (no regressions)
3. `turbo.json` outputs array contains `"public/r/**"`
4. Live registry status documented (200 or deployment-pending)
</verification>

<success_criteria>
- ApplyActionBar renders `blink apply eslint-flat-config` (not `blink apply config/eslint-flat-config`)
- `type` prop removed from ApplyActionBar component and its caller
- turbo.json build outputs include `public/r/**` for registry cache correctness
- Live registry endpoint status checked and documented
- All existing tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/20-fix-integration-gaps/20-01-SUMMARY.md`
</output>
