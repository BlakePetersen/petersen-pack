---
phase: 21-artax-ui-restructure-theming
plan: 03
type: execute
wave: 3
depends_on: ["21-02"]
files_modified:
  - packages/artax-ui/src/components/atoms/badge/badge.tsx
  - packages/artax-ui/src/components/atoms/button/button.tsx
  - packages/artax-ui/src/components/atoms/input/input.tsx
  - packages/artax-ui/src/components/atoms/separator/separator.tsx
  - packages/artax-ui/src/components/atoms/copy-button/copy-button.tsx
  - packages/artax-ui/src/components/atoms/toggle/toggle.tsx
  - packages/artax-ui/src/components/atoms/toggle/toggle-interactive.tsx
  - packages/artax-ui/src/components/molecules/card/card.tsx
  - packages/artax-ui/src/components/molecules/callout/callout.tsx
  - packages/artax-ui/src/components/molecules/code-block/code-block.tsx
  - packages/artax-ui/src/components/molecules/tabs/tabs.tsx
  - packages/artax-ui/src/components/molecules/tabs/tabs-interactive.tsx
  - packages/artax-ui/src/components/molecules/tooltip/tooltip.tsx
  - packages/artax-ui/src/components/molecules/tooltip/tooltip-interactive.tsx
  - packages/artax-ui/src/components/molecules/table/table.tsx
  - packages/artax-ui/src/components/organisms/accordion/accordion.tsx
  - packages/artax-ui/src/components/organisms/accordion/accordion-interactive.tsx
  - packages/artax-ui/src/components/organisms/dialog/dialog.tsx
  - packages/artax-ui/src/components/organisms/dialog/dialog-interactive.tsx
  - packages/artax-ui/src/components/organisms/dropdown/dropdown-menu.tsx
  - packages/artax-ui/src/components/organisms/dropdown/dropdown-interactive.tsx
  - packages/artax-ui/src/mdx/components.tsx
  - packages/artax-ui/src/styles/theme.css
  - packages/artax-ui/tests/token-usage.test.ts
autonomous: true
requirements:
  - FOUND-04

must_haves:
  truths:
    - "No artax-ui component file contains terminal-*, amber-accent, or hardcoded hex color values in Tailwind classes"
    - "All components use semantic token utilities (bg-background, text-foreground, bg-primary, etc.)"
    - "mdx/components.tsx uses semantic tokens — no inline rgba() styles or terminal-* classes"
    - "Legacy --color-terminal-* and --color-amber-accent tokens are removed from theme.css @theme block"
    - "pnpm turbo build succeeds after full token migration"
  artifacts:
    - path: "packages/artax-ui/src/styles/theme.css"
      provides: "Clean static-only tokens (no legacy color tokens)"
    - path: "packages/artax-ui/tests/token-usage.test.ts"
      provides: "Test verifying no hardcoded colors in any component"
  key_links:
    - from: "packages/artax-ui/src/components/**/*.tsx"
      to: "packages/artax-ui/src/styles/globals.css"
      via: "Tailwind semantic utility classes (bg-background, text-foreground, etc.)"
      pattern: "bg-background|text-foreground|bg-primary|text-primary|border-border|bg-card|text-muted-foreground|bg-muted"
    - from: "packages/artax-ui/src/mdx/components.tsx"
      to: "packages/artax-ui/src/styles/globals.css"
      via: "Tailwind semantic utility classes"
      pattern: "text-foreground|bg-card|border-border"
---

<objective>
Migrate all 21 component files and mdx/components.tsx from terminal-* token utilities to semantic Tailwind utilities, then remove legacy tokens from theme.css.

Purpose: Complete the token migration so all artax-ui components render correctly in both light and dark modes using the dual-mode CSS custom property system from Plan 02.
Output: All components using semantic tokens, no hardcoded colors, legacy tokens removed from theme.css.
</objective>

<execution_context>
@/Users/blake/.claude/get-shit-done/workflows/execute-plan.md
@/Users/blake/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/21-artax-ui-restructure-theming/21-CONTEXT.md
@.planning/phases/21-artax-ui-restructure-theming/21-RESEARCH.md
@.planning/phases/21-artax-ui-restructure-theming/21-01-SUMMARY.md
@.planning/phases/21-artax-ui-restructure-theming/21-02-SUMMARY.md

<interfaces>
<!-- Token migration map from 21-RESEARCH.md Pattern 2 -->
| Old Utility | New Utility | Notes |
|-------------|-------------|-------|
| bg-terminal-bg | bg-background | Main background |
| bg-terminal-surface | bg-card | Surface/card background |
| bg-terminal-active | bg-muted | Active/hover background |
| border-terminal-border | border-border | All borders |
| text-terminal-text | text-foreground | Primary text |
| text-terminal-muted | text-muted-foreground | Muted/dimmed text |
| text-terminal-secondary | text-secondary-foreground | Secondary text |
| text-terminal-disabled | text-muted-foreground | Disabled (same as muted) |
| bg-amber-accent / text-amber-accent | bg-primary / text-primary | Amber accent |
| ring-amber-accent | ring-ring | Focus rings |
| border-l-terminal-cyan | border-l-info | Callout: info variant |
| border-l-terminal-red | border-l-destructive | Callout: error variant |
| border-l-terminal-green | border-l-success | Callout: success variant |
| bg-amber-500/10 | bg-primary/10 | Code highlight |

<!-- Token names for semantic surface colors -->
| Old pattern | New pattern |
|-------------|-------------|
| rgba(6, 182, 212, 0.06) inline style | var(--surface-info) or bg-[var(--surface-info)] |
| rgba(245, 158, 11, 0.06) | var(--surface-warning) or bg-[var(--surface-warning)] |
| rgba(16, 185, 129, 0.06) | var(--surface-success) or bg-[var(--surface-success)] |

<!-- Semantic status color tokens available from globals.css -->
:root: --success: #059669, --info: #0891B2, --warning: #D97706, --destructive: #DC2626
[data-theme=dark]: --success: #10B981, --info: #06B6D4, --warning: #F59E0B, --destructive: #EF4444
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Migrate all component token references and remove legacy tokens</name>
  <files>
    packages/artax-ui/src/components/atoms/badge/badge.tsx
    packages/artax-ui/src/components/atoms/button/button.tsx
    packages/artax-ui/src/components/atoms/input/input.tsx
    packages/artax-ui/src/components/atoms/separator/separator.tsx
    packages/artax-ui/src/components/atoms/copy-button/copy-button.tsx
    packages/artax-ui/src/components/atoms/toggle/toggle.tsx
    packages/artax-ui/src/components/atoms/toggle/toggle-interactive.tsx
    packages/artax-ui/src/components/molecules/card/card.tsx
    packages/artax-ui/src/components/molecules/callout/callout.tsx
    packages/artax-ui/src/components/molecules/code-block/code-block.tsx
    packages/artax-ui/src/components/molecules/tabs/tabs.tsx
    packages/artax-ui/src/components/molecules/tabs/tabs-interactive.tsx
    packages/artax-ui/src/components/molecules/tooltip/tooltip.tsx
    packages/artax-ui/src/components/molecules/tooltip/tooltip-interactive.tsx
    packages/artax-ui/src/components/molecules/table/table.tsx
    packages/artax-ui/src/components/organisms/accordion/accordion.tsx
    packages/artax-ui/src/components/organisms/accordion/accordion-interactive.tsx
    packages/artax-ui/src/components/organisms/dialog/dialog.tsx
    packages/artax-ui/src/components/organisms/dialog/dialog-interactive.tsx
    packages/artax-ui/src/components/organisms/dropdown/dropdown-menu.tsx
    packages/artax-ui/src/components/organisms/dropdown/dropdown-interactive.tsx
    packages/artax-ui/src/mdx/components.tsx
    packages/artax-ui/src/styles/theme.css
    packages/artax-ui/tests/token-usage.test.ts
  </files>
  <behavior>
    - Test: No .tsx file in src/components/ contains "terminal-" in a className or cn() argument
    - Test: No .tsx file in src/components/ contains "amber-accent" in a className or cn() argument
    - Test: src/mdx/components.tsx contains no "terminal-" class references
    - Test: src/mdx/components.tsx contains no inline rgba() style values
    - Test: theme.css @theme block contains no --color-terminal-* tokens
    - Test: theme.css @theme block contains no --color-amber-accent token
    - Test: theme.css @theme block contains no --color-surface-* tokens (these are in globals.css now)
  </behavior>
  <action>
    **Create token-usage test first (RED):**
    Create `packages/artax-ui/tests/token-usage.test.ts`:
    - Recursively scan all .tsx files under `src/components/`
    - Assert no file content matches `terminal-` in what looks like a Tailwind class context (avoid false positives on comments/ABOUTME)
    - Assert no file content matches `amber-accent` in class context
    - Read `src/mdx/components.tsx` and assert no `terminal-` class references and no inline `rgba(` in style attributes
    - Read `src/styles/theme.css` and assert the @theme block contains no `--color-` tokens

    **Migrate all component files (GREEN):**
    Apply the migration map from the interfaces section above to every component file. This is a systematic find-and-replace across all 21 component files + mdx/components.tsx.

    For each file, read it, replace ALL terminal-*/amber-* utility classes with semantic equivalents per the map. Key replacements:
    - `bg-terminal-bg` → `bg-background`
    - `bg-terminal-surface` → `bg-card`
    - `bg-terminal-active` → `bg-muted`
    - `border-terminal-border` → `border-border`
    - `text-terminal-text` → `text-foreground`
    - `text-terminal-muted` → `text-muted-foreground`
    - `text-terminal-secondary` → `text-secondary-foreground`
    - `text-terminal-disabled` → `text-muted-foreground`
    - `bg-amber-accent` → `bg-primary`
    - `text-amber-accent` → `text-primary`
    - `ring-amber-accent` → `ring-ring`
    - `bg-amber-500/10` → `bg-primary/10`

    **Special cases:**
    - `callout.tsx`: `border-l-terminal-cyan` → `border-l-info`, `border-l-terminal-red` → `border-l-destructive`, `border-l-terminal-green` → `border-l-success`. Surface colors: `bg-surface-info`, `bg-surface-warning`, `bg-surface-success` (these use the --color-surface-* tokens from @theme which are being migrated to --surface-* in globals.css — verify Tailwind v4 generates the correct utilities).
    - `mdx/components.tsx`: Has an AuthorNote with `style={{ backgroundColor: 'rgba(6, 182, 212, 0.06)' }}` — replace with `className="bg-[var(--surface-info)]"` or equivalent Tailwind utility. Also migrate all `text-terminal-*`, `bg-terminal-*`, `border-terminal-*` classes.
    - If any component has `text-terminal-cyan`, `text-terminal-red`, `text-terminal-green` (semantic status colors), replace with `text-info`, `text-destructive`, `text-success`.

    **IMPORTANT — Tailwind v4 utility generation (Pitfall 3 from research):**
    For tokens like `--success`, `--info`, `--warning` that don't follow the `--color-*` namespace, Tailwind v4 may not auto-generate `bg-success`, `text-info` etc. If needed, register these in the `@theme` block as `@theme inline`:
    ```css
    @theme inline {
      --color-success: var(--success);
      --color-info: var(--info);
      --color-warning: var(--warning);
      --color-surface-info: var(--surface-info);
      --color-surface-warning: var(--surface-warning);
      --color-surface-success: var(--surface-success);
    }
    ```
    Or use Tailwind arbitrary value syntax: `bg-[var(--surface-info)]`. Test which approach generates correct CSS.

    **Remove legacy tokens from theme.css:**
    After ALL component classes are migrated, remove all `--color-terminal-*`, `--color-amber-accent`, and `--color-surface-*` tokens from the `@theme` block in theme.css. The @theme block should only contain static tokens (fonts, radii) plus any `@theme inline` registrations needed for Tailwind utility generation.

    **Also update the theme.test.ts** from Plan 02 — remove the "temporary legacy tokens" test expectations that were marked for removal in this plan. The test should now verify NO `--color-terminal-*` tokens exist.

    Run `pnpm turbo build` after migration to verify no broken utilities.
  </action>
  <verify>
    <automated>cd /Users/blake/Sites/petersen-group && pnpm turbo build && cd packages/artax-ui && pnpm test -- tests/token-usage.test.ts tests/theme.test.ts -x 2>&1 | tail -40</automated>
  </verify>
  <done>All 21 component files and mdx/components.tsx use semantic token utilities only. No terminal-*, amber-accent, or hardcoded hex colors remain in class attributes. Legacy --color-terminal-* tokens removed from theme.css. All tests pass. pnpm turbo build green.</done>
</task>

<task type="auto">
  <name>Task 2: Create dev components preview page</name>
  <files>
    apps/blakepetersen.io/src/app/dev/components/page.tsx
  </files>
  <action>
    Create a dev-only page at `apps/blakepetersen.io/src/app/dev/components/page.tsx` for visual validation of all components in both light and dark themes simultaneously.

    **Production guard:** At the top of the page component, check `process.env.NODE_ENV !== 'development'` and return `notFound()` from `next/navigation` if in production.

    **Page structure:**
    - Import all components from `artax-ui` barrel
    - Render two side-by-side columns: one with `data-theme="light"` wrapper, one with `data-theme="dark"` wrapper
    - Each column shows all components with fresh, simple examples (NOT translated from old Storybook stories):
      - Badge: default, outline, secondary variants
      - Button: default, outline, ghost variants with different states
      - Input: text input with placeholder
      - Card: with header, content, footer
      - Callout: info, warning, error variants with sample text
      - CodeBlock: short code sample
      - Table: 3-4 rows of sample data
      - Separator
      - Accordion: 2-3 items
      - Tabs: 3 tabs with content
      - Dialog trigger + content
      - Dropdown trigger + menu
      - Tooltip on hover
      - Toggle

    - Use ThemeProvider to wrap each column section
    - Minimal layout — show components vertically within each column with section headings
    - Add ABOUTME comments at top of file

    This page is temporary — will be removed when Phase 22 (reference site) ships, as noted in CONTEXT.md.
  </action>
  <verify>
    <automated>cd /Users/blake/Sites/petersen-group && pnpm turbo build 2>&1 | tail -10</automated>
  </verify>
  <done>Dev components page exists at /dev/components in blakepetersen.io. Returns 404 in production. Shows all components side-by-side in light and dark themes. Build succeeds.</done>
</task>

</tasks>

<verification>
- `cd packages/artax-ui && pnpm test -- --verbose` — ALL tests pass (boundaries, theme, theme-provider, token-usage, storybook-removal, component tests)
- `pnpm turbo build` — full monorepo build succeeds
- `grep -r 'terminal-' packages/artax-ui/src/components/ --include='*.tsx' | grep -v ABOUTME | grep -v '//'` — returns empty (no terminal-* in component class attributes)
- `grep -r 'amber-accent' packages/artax-ui/src/components/ --include='*.tsx'` — returns empty
- `grep -r 'terminal-' packages/artax-ui/src/mdx/components.tsx | grep -v ABOUTME | grep -v '//'` — returns empty
- `pnpm eslint packages/artax-ui/src/ --rule 'import-x/no-cycle: error'` — passes
</verification>

<success_criteria>
- Zero terminal-*, amber-accent, or hardcoded hex color values in any artax-ui component Tailwind classes
- All components use semantic utilities (bg-background, text-foreground, bg-primary, etc.)
- mdx/components.tsx fully migrated (no inline rgba, no terminal-* classes)
- Legacy --color-terminal-* tokens removed from theme.css
- Dev components page working at /dev/components showing both themes
- Full test suite and build green
</success_criteria>

<output>
After completion, create `.planning/phases/21-artax-ui-restructure-theming/21-03-SUMMARY.md`
</output>
